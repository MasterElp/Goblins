#pragma once

// Экраны приложения. Меню — точка входа после запуска; пункты меню ведут
// в соответствующий экран. Назад — по Esc, из любого экрана в меню.
//
// "Simulation" в меню ведёт не сразу в мир, а на экран выбора мира
// (WorldSelect): симулируется всегда какой-то конкретный сохранённый мир,
// поэтому его сначала нужно выбрать (или создать новый).
enum class AppScreen {
    MainMenu,
    Settings,
    WorldGeneration,
    WorldSelect,
    Simulation,
};

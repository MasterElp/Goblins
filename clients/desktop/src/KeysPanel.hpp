#pragma once

#include <raylib.h>

#include "MapTexture.hpp"

// Вкладка "Keys": всё управление экраном мира одним списком.
//
// Раньше это была строка мелким текстом по нижнему краю карты, а состояние
// слоёв — вторая строка поверх самой карты. Обе мешали смотреть на мир и
// обе всё равно не вмещали ни клики мышью, ни то, что каждая цифра
// включает. В панели места достаточно, а карта остаётся картой.
//
// Слои показаны здесь же со своим состоянием (включён/выключен) не для
// полноты списка: выключенный слой меняет цвет каждой клетки, и не видеть,
// какие из семи сейчас участвуют в картинке, — значит смотреть на карту
// вслепую. Раз надписей на карте больше нет, единственное место, где это
// состояние может жить, — рядом с самими клавишами.
namespace KeysPanel {

void draw(Rectangle bounds, const MapTexture::Layers& layers);

} // namespace KeysPanel

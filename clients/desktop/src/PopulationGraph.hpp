#pragma once

#include <raylib.h>

#include "NetworkClient.hpp"

// Численность видов по времени: трава (сколько клеток занимает вид) и
// травоядные (сколько особей) — двумя панелями с общей осью тиков.
//
// Летопись ведёт сервер и присылает её вместе с состоянием мира
// (WorldState::populationHistory, см. server/PopulationHistory.hpp), а
// клиент только рисует. Копить её здесь, у наблюдателя, значило бы
// показывать не жизнь мира, а время, которое на него смотрели: история
// обрывалась бы на каждом переподключении и начиналась бы с нуля у каждого
// клиента, хотя мир к тому моменту прожил тысячи тиков.
//
// Поэтому и состояния у модуля нет вовсе: всё, что рисуется, приходит
// снаружи одним снимком.
namespace PopulationGraph {

// Нарисовать панель в этих границах. allowHover — можно ли читать
// значения под курсором (ложь, когда поверх открыт модальный диалог).
void draw(const WorldState& state, Rectangle bounds, bool allowHover);

} // namespace PopulationGraph

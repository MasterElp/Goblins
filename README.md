World simulator
# Главная идея
- Проект представляет собой автономную симуляцию живого мира.
- Основой проекта является не игровой процесс, а моделирование мира, который способен существовать, развиваться и изменяться независимо от присутствия игрока.
- Игрок не является центром мира. Он лишь один из возможных наблюдателей или участников происходящих событий.

# Goblins

Симуляция мира. Архитектура и философия проекта — в `docs/`.

## Сервер

### Сборка

```
mkdir build && cd build
cmake ..
cmake --build . -j
```

Зависимости (EnTT, nlohmann/json, IXWebSocket) подтягиваются автоматически
через CPM.cmake при конфигурации — интернет нужен только на этом шаге.
Системных зависимостей (OpenSSL, zlib) не требуется — TLS и сжатие
WebSocket-сообщений отключены за ненадобностью на этом этапе (сервер
рассчитан на локальную сеть).

### Запуск

```
./server/goblins_server [кол-во тиков] [порт]
```

По умолчанию — 10 тиков, порт 9002. Программа создаёт Область 100x100,
случайно размещает 40 непроходимых булыжников (без коллизий по тайлам),
поднимает WebSocket-сервер и запускает игровой цикл, рассылая номер
каждого тика подключённым клиентам.

### Протокол (WebSocket, JSON)

Сервер → клиент, сразу при подключении:
```json
{"type": "world_snapshot", "area": {"width": 100, "height": 100}, "tick": 0,
 "boulders": [{"x": 12, "y": 34}, ...]}
```

Сервер → клиент, после каждого тика:
```json
{"type": "tick", "tick": 5}
```

## Десктопный клиент

Отдельный CMake-проект в `clients/desktop` — собирается независимо от
сервера. Чистый C++, без GUI-фреймворка: рендер — в терминал, зависимости
те же, что у сервера (IXWebSocket + nlohmann/json), подтягиваются сами
через CPM.cmake. Устанавливать Qt или что-либо ещё не нужно.

### Сборка

```
cd clients/desktop
mkdir build && cd build
cmake ..
cmake --build . -j
```

### Запуск

Сначала должен быть запущен сервер. Затем:

```
./goblins_desktop localhost:9002
```

Адрес можно не указывать — по умолчанию `localhost:9002`.

В терминале рисуется окно карты 60x25 символов (`.` — пустой тайл, `#` —
булыжник), сверху — текущий тик и координаты окна просмотра. Управление:
`W`/`A`/`S`/`D` — прокрутка на 5 тайлов, `Q` — выход.

## Структура

- `core/` — ядро симуляции: ECS (EnTT), компоненты, системы, игровой цикл, генерация мира. Не зависит от server/clients.
- `server/` — исполняемый файл, оборачивающий core: игровой цикл + WebSocket-слой (IXWebSocket + nlohmann/json).
- `clients/desktop/` — консольный клиент на чистом C++, отображающий состояние мира. Получает данные только по WebSocket-протоколу сервера.
- `clients/web/` — будущий браузерный клиент.
- `docs/` — архитектурные документы проекта.

## Changelog

История изменений — в `CHANGELOG.md`.

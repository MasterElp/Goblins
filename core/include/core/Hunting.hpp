#pragma once

#include <algorithm>
#include <cstdint>
#include <cstdlib>
#include <span>
#include <vector>

#include "core/Path.hpp"
#include "core/Random.hpp"
#include "core/Scale.hpp"
#include "core/Strike.hpp"

namespace goblins {

// Охота — ОДИН закон мира на систему и на наблюдателя, по той же причине,
// что голод и жажда в core/Needs.hpp: пользуются им уже двое — AnimalSystem
// (чтобы хищник выбрал, за кем идти) и сервер (чтобы показать эту дорогу на
// карте). Две копии одного закона разъезжаются молча, а нарисованная
// дорога, по которой зверь на самом деле не идёт, хуже ненарисованной:
// именно по ней и судят, работает ли охота вообще.
//
// Здесь только то, за кем хищник пойдёт. Сама дорога — общая для всех, кто
// её ищет, и живёт в core/Path.hpp; удар, дележ туши и ходьба остаются в
// AnimalSystem — это уже не знание зверя, а события мира.

// С какого голода хищник выходит на охоту. Порог выше общего kDesireFloor
// намеренно: слегка проголодавшийся зверь подберёт падаль, если она рядом,
// но гнаться за живой добычей не станет. Сытый хищник, пропускающий добычу
// мимо, — не поблажка стаду, а единственное, что вообще удерживает его
// численность: пока хищники убивали при любом голоде, они выбивали стадо
// подчистую и вымирали следом, сколько ни крути прочие числа.
constexpr int kHuntHunger = 350;

// Ниже этого мяса глодать нечего: хищник просто не считает такую тушу едой,
// иначе он сидел бы у обглоданных костей вместо охоты.
constexpr int kMinBiteMeat = 50;

// Какую долю собственного тела может весить добыча. Крупнее — хищник за неё
// не берётся вовсе (проверка ниже).
//
// Доля, а не "не крупнее себя", и это главное число охоты. Урон считается
// отношением размеров (core/Strike.hpp), поэтому бой РОВНИ симметричен: оба
// валят друг друга с трёх ударов, и решает его меткость, то есть жребий. А
// положение сторон в этом бою совсем не равное — хищник обязан выигрывать
// схватку за схваткой, чтобы есть, травоядному же достаточно выиграть одну.
// Войну на истощение при таких правилах сторона из шестидесяти проигрывает
// стороне из четырёхсот при любом выборе цели: шестьдесят хищников таяли до
// двух за триста тиков в мире, полном добычи.
//
// При семи десятых отношение выходит 1.43 в пользу хищника: он снимает
// пятьсот здоровья за удар и получает двести сорок пять — два удара против
// четырёх. Победа решительная, но не даровая: три-четыре тика он под зубами
// и рогами, и неудачная охота обходится ему дорого.
//
// Отсюда же и вся картина еды в этом мире: берут молодых, мелких и
// отбившихся, а взрослый крупный зверь неуязвим, пока держится своего
// размера и своего стада.
constexpr int kHuntPreyShare = 700;

// Насколько величина добычи отпугивает при выборе цели — "виртуальные шаги"
// непривлекательности за то, что жертва вровень с самим хищником.
//
// Слагаемое малое, и это не поблажка: тех, кто крупнее самого хищника, оно
// уже не касается — их отсеивает прямой пропуск ниже. Осторожность лишь
// выбирает среди тех, кто и так мельче: мелкая добыча и валится быстрее, и
// сдачи даёт слабее.
//
// Прежде здесь стояло 24 с обещанием, что добыча вровень с хищником
// "отодвигается дальше, чем он видит". Обещание было пустым: очки только
// РАНЖИРУЮТ добычу друг против друга и никого не исключают — сравнения с
// радиусом видимости в законе нет вовсе. Исключать надо было запретом, и
// теперь он есть.
constexpr int kHuntCaution = 6;

// Насколько отпугивает КОМПАНИЯ добычи — "виртуальные шаги" за каждого
// соседа, стоящего с ней рядом.
//
// Хищник ищет одиночку, и это не хитрость поведения, а условие того, чтобы
// охота вообще была возможна. Съесть кого-нибудь можно только подойдя
// вплотную, а бьёт хищника всякий, у кого зубы в соседней клетке
// (AnimalSystem, Desire::Flee). Стадо же с некоторых пор ходит кучей
// (WalkHerd, core/Walk.hpp): вокруг травоядного в среднем без малого трое
// своих, местами тринадцать. Вошедший в такую кучу получал больше тысячи
// урона за тик при здоровье в тысячу — то есть умирал в тик входа, не успев
// укусить никого. Двадцать хищников кончались за сто тридцать тиков в мире,
// полном добычи.
//
// Вес крупный, крупнее осторожности к величине: один лишний защитник
// опаснее, чем разница в размере, потому что бьют они одновременно и урон
// складывается. Отсюда и вся картина охоты: хищник обходит стадо и берёт
// отбившегося — а стадо, сбившись плотнее, само себя защищает. Ни одного
// нового понятия для этого не понадобилось: стадность уже есть, удар уже
// есть, осталось только смотреть на соседей при выборе.
constexpr int kHuntCompany = 10;

// В каком радиусе от добычи считаются её соседи. Не kStrikeReach, а вдвое
// больше: хищник встанет ВПЛОТНУЮ к жертве, и тогда в его собственную
// досягаемость попадёт всякий, кто стоял в двух клетках от неё.
constexpr int kCompanyRadius = 2;

// Докуда хищник замечает добычу, стоящую под деревом. Одна клетка — то
// есть ровно на длину зубов (kStrikeReach, core/Strike.hpp): под кроной жертву не
// высматривают, на неё натыкаются.
//
// Это и делает рощу убежищем, а не украшением. Без укрытия у стада есть
// один способ уцелеть — бежать быстрее, и весь бюджет травоядного уходит в
// скорость; с укрытием появляется второй, и он не покупается бюджетом
// вовсе, а берётся с карты. Заодно роща становится местом, за которое
// стоит держаться: пастись у деревьев безопаснее, чем в чистом поле, и
// стадо само собой оказывается там, где ему есть куда деться.
//
// Полной неуязвимости укрытие не даёт: хищник, забредший вплотную,
// увидит и укушенное. Роща — не стена, а полог.
constexpr int kCoverSight = 1;

// Добыча глазами хищника: где стоит, как быстро бежит и насколько велика.
// Больше он о ней ничего не знает — ни возраста, ни запаса сил
// (02_CorePrinciples.md, п.6). Величина видна точно так же, как и бег, и в
// этом вся разница с прежними рогами: размер — это то, что видно снаружи, а
// меткость (hit_chance) снаружи не разглядеть, и хищник её не учитывает.
struct HuntPrey {
    int x = 0;
    int y = 0;
    int speed = 0;
    // Размер тела (core/Body.hpp), а не размер вида: телёнок лося — добыча,
    // взрослый лось — нет.
    int size = 0;
    // Сколько своих стоит рядом с ней (kCompanyRadius). Столько защитников
    // и встретит хищника, когда он подойдёт: бьёт всякий, у кого зубы в
    // соседней клетке.
    int company = 0;
    // Стоит ли добыча под деревом. Не свойство самой добычи, а свойство
    // клетки под ней — но хищник знает о жертве ровно то, что видит, и
    // "не вижу за кроной" ему сообщается здесь же, вместе с остальным
    // видимым (02_CorePrinciples.md, п.6).
    bool hidden = false;
};

// Сам хищник: где стоит, докуда видит, как быстро бежит, насколько голоден
// и насколько велик сам. Своя величина нужна затем, что опасность добычи —
// величина не сама по себе, а по отношению к собственной.
struct Hunter {
    int x = 0;
    int y = 0;
    int perception = 1;
    int speed = 0;
    int hunger = 0;
    int size = kFull;
};

// Что хищник выбрал: живую добычу, тушу или ничего.
struct HuntChoice {
    enum class Kind { None, Prey, Carcass };
    Kind kind = Kind::None;
    int prey = -1;        // номер в списке добычи (при Kind::Prey)
    int x = 0;
    int y = 0;
    bool atTeeth = false; // добыча уже в пределах укуса: бить, а не идти
};

// За кем пойдёт хищник. carcassAt(x, y) — сколько мяса лежит на клетке.
// random — розыгрыш для равных находок (см. ниже); передаётся копией,
// поэтому наблюдатель, собрав его тем же способом, что и система, получит
// тот же выбор.
template <typename CarcassAt>
HuntChoice chooseHuntTarget(const Reach& reach, const Hunter& hunter, std::span<const HuntPrey> prey,
                            CarcassAt&& carcassAt, std::uint64_t random) {
    const int sight = std::max(1, hunter.perception);
    HuntChoice choice;

    // --- Живая добыча ---
    // Ближе всех и первой: гнаться за той, что дальше, когда рядом стоит
    // эта, бессмысленно. Но только если хищник и вправду голоден.
    int preyIndex = -1;
    int bestScore = 0;
    for (std::size_t b = 0; hunter.hunger >= kHuntHunger && b < prey.size(); ++b) {
        // За тем, кто быстрее, гнаться незачем: догнать его нельзя, а силы
        // уйдут. Скорость чужого бега — ровно то знание, которое у хищника
        // есть: он её видит, в отличие от чужого возраста или запаса сил.
        //
        // Без этого правила хищник выбирал ближайшую добычу и гнался за
        // ней, даже если она заведомо уходила; на безнадёжные погони уходила
        // вся энергия, и первое поголовье вымирало за тысячу тиков в мире,
        // полном добычи. Заодно у стада появляется смысл вкладывать бюджет
        // в скорость: быстрого не преследуют вовсе.
        if (prey[b].speed >= hunter.speed) {
            continue;
        }
        // За тем, кто не по зубам, гнаться не незачем, а смертельно: удар
        // считается отношением размеров (core/Strike.hpp), и добыча вровень
        // валит хищника ровно так же быстро, как он её, — а подойти вплотную
        // обязан он. Порог долевой, см. kHuntPreyShare.
        //
        // Запрет, а не очки, и это тот же случай, что и скорость строкой
        // выше: очки лишь ранжируют добычу друг против друга, и когда вся
        // округа не по зубам, лучшая из плохих всё равно окажется выбрана.
        // Такой выбор — не "рискнул", а "пошёл умирать".
        //
        // Величина видна снаружи, как и бег, поэтому знать её хищнику можно
        // (02_CorePrinciples.md, п.6).
        if (prey[b].size * kFull > hunter.size * kHuntPreyShare) {
            continue;
        }
        const int dx = prey[b].x - hunter.x;
        const int dy = prey[b].y - hunter.y;
        if (dx * dx + dy * dy > sight * sight) {
            continue;
        }
        // Под кроной добычу не высматривают: сколь угодно зоркий хищник
        // заметит спрятавшегося, только подойдя вплотную (kCoverSight).
        if (prey[b].hidden && (std::abs(dx) > kCoverSight || std::abs(dy) > kCoverSight)) {
            continue;
        }

        // Здесь дорога и решает. За добычей, до которой дороги нет, хищник
        // не гонится вовсе: она для него не добыча, а вид. Прежде он брал
        // ближайшую по прямой, шёл к ней, упирался в воду, а на следующий
        // тик выбирал её же, одну и ту же, — и так до голодной смерти на
        // берегу, пока по его сторону реки паслось другое стадо.
        //
        // И "ближе" считается дорогой, а не отрезком: для идущего ногами
        // ближе тот, до кого меньше шагов. Добыча за излучиной реки близка
        // глазу и далека ногам, и гнаться надо не за ней.
        //
        // Соседняя клетка — исключение: до неё дороги не требуется, в неё
        // кусают. Иначе хищник упустил бы жертву, которую залило
        // разлившейся водой у него под носом, — зубы туда достают.
        const bool atTeeth = strikeReaches(hunter.x, hunter.y, prey[b].x, prey[b].y);
        int distance = kStrikeReach;
        if (!atTeeth) {
            distance = reach.steps(prey[b].x, prey[b].y);
            if (distance < 0) {
                continue;
            }
        }

        // Величина добычи тоже видна, и хищник это учитывает — не запретом,
        // а осторожностью: крупная становится "как будто дальше" во столько
        // раз, во сколько она к нему велика. Правило одно на клетку
        // досягаемости зубов и на клетку в глубине видимости: крупная
        // соседняя не обязана автоматически выигрывать у мелкой в двух шагах.
        const int score = distance + kHuntCaution * prey[b].size / std::max(1, hunter.size) +
                          kHuntCompany * prey[b].company;
        if (preyIndex >= 0 && score >= bestScore) {
            continue;
        }
        preyIndex = static_cast<int>(b);
        bestScore = score;
    }

    if (preyIndex >= 0) {
        const auto& target = prey[static_cast<std::size_t>(preyIndex)];
        choice = HuntChoice{HuntChoice::Kind::Prey, preyIndex, target.x, target.y,
                            strikeReaches(hunter.x, hunter.y, target.x, target.y)};
        if (choice.atTeeth) {
            return choice; // бить, а не идти: дорога такому уже не нужна
        }
    }

    // --- Падаль ---
    // Видимая падаль важнее видимой добычи: зачем гнаться, когда рядом
    // лежит мясо. Хищнику всё равно, кто убил ту тушу (см.
    // PredatorComponent).
    //
    // Правило не косметическое. Пока хищник шёл к тому, что ближе, он
    // бросал недоеденную тушу ради свежей добычи и убивал куда больше, чем
    // съедал: по карте лежали десятки почти нетронутых туш, а стадо тем
    // временем выбивалось под ноль — и следом вымирали сами хищники.
    // "Сначала доешь" превращает лишнее убийство в редкость, а не в
    // правило.
    //
    // Годной считается только та туша, до которой ведёт дорога, и мерится
    // она тоже дорогой: берег на той стороне реки проходим, а хода на него
    // нет. Без этой проверки хищник вставал у самой воды напротив
    // недосягаемой туши и голодал насмерть — в мире была еда, до которой он
    // не мог дойти, и он не умел этого понять.
    //
    // Из одинаково далёких туш выбор бросается жребием, а не достаётся
    // первой по обходу: обход идёт с левого верхнего угла квадрата
    // видимости, и без жребия хищники дружно уходили бы вверх и влево — не
    // потому, что там лучше, а потому, что цикл начинается оттуда.
    int carcassDistance = -1;
    int ties = 0;
    for (int dy = -sight; dy <= sight; ++dy) {
        for (int dx = -sight; dx <= sight; ++dx) {
            const int nx = hunter.x + dx;
            const int ny = hunter.y + dy;
            const int distance = reach.steps(nx, ny);
            if (distance < 0 || carcassAt(nx, ny) <= kMinBiteMeat) {
                continue;
            }
            if (ties > 0 && distance > carcassDistance) {
                continue;
            }
            if (ties > 0 && distance == carcassDistance) {
                ++ties;
                if (randomBelow(random, static_cast<std::uint64_t>(ties)) != 0) {
                    continue;
                }
            } else {
                ties = 1;
            }
            carcassDistance = distance;
            choice.kind = HuntChoice::Kind::Carcass;
            choice.prey = -1;
            choice.atTeeth = false;
            choice.x = nx;
            choice.y = ny;
        }
    }

    return choice;
}

} // namespace goblins

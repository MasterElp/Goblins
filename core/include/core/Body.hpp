#pragma once

#include <algorithm>
#include <cstdint>

#include "core/Scale.hpp"
#include "core/components/AnimalComponent.hpp"
#include "core/components/AnimalGenomeComponent.hpp"

namespace goblins {

// Тело живого — ОДИН закон мира на всех, у кого оно есть.
//
// Живёт здесь, а не внутри AnimalSystem, по той же причине, что голод и
// жажда живут в core/Needs.hpp: пользуются им уже двое. Расход на
// существование, взросление, пищеварение, здоровье и смерть у гоблина те же
// самые, что у животного, — различаются они не телом, а тем, как принимают
// решения. Две копии этого закона разъехались бы молча и в самом неприятном
// месте: в мире, где одни существа стареют по одному правилу, а другие по
// почти такому же.
//
// Имя AnimalComponent при этом осталось прежним, хотя означает оно "тело
// живого", а не "животное". Переименование задевает три десятка файлов,
// формат сохранения и все панели наблюдения ради нуля изменений в
// поведении, и потому отложено осознанно, а не забыто.
//
// Чего здесь НЕТ и почему:
//   - удар и хромота: это не то, что тело делает само, а то, что с ним
//     делает другое тело, — core/Strike.hpp;
//   - память ног (fadeWalkMemory): core/Walk.hpp;
//   - страх: он считается не из своего тела, а из чужого присутствия (см.
//     core/Needs.hpp).

// Насколько маленькое животное "меньше ест": и расход на жизнь, и укус, и
// цена шага, и скорость пищеварения, и сила удара умножаются на размер (от
// kMinSizeShare у новорождённого до 1 у взрослого). У растений такой же
// был и пропал вместе с тем единственным, что он делил (подсушиванием
// клетки); здесь он делит слишком многое, чтобы пропасть. Без него
// детёныш объедал бы луг наравне со взрослым и почти никогда не выживал
// бы на бедной земле.
constexpr int kMinSizeShare = 300;

// Сколько энергии даёт единица биомассы съеденного. Число связывает две
// шкалы, которые иначе не сопоставимы: развитость растения живёт в [0, 1],
// а расход животного — в единицах за тик. Оно же переводит и мясо: биомасса
// травы и биомасса туши меряются одной шкалой, поэтому травоядное и хищник
// считают энергию одинаково, а разница между диетами остаётся в том, ГДЕ её
// взять, а не в том, сколько её в куске.
//
// Значение меньше прежнего ровно настолько, насколько раньше его в среднем
// урезало пищеварение генома (digestion, 0.3..1.0). Самой этой черты
// больше нет: невидимый множитель, за который вид платил бюджетом и о
// котором в мире нельзя было узнать иначе как по скорости насыщения.
// Энергии за одну тысячную биомассы (kFull биомассы = взрослый куст).
constexpr int kEnergyPerBiomass = 26;

// Сколько воды животное получает из самой еды: трава сочная, в туше есть
// кровь, и наевшийся долго может не искать реку. Одно число на обе диеты, а
// не по своему на каждую: две величины были равны с самого начала, и разной
// их никто никогда не делал.
//
// Число заметное, а не символическое: хищник уходит за добычей далеко от
// воды, и если бы поить его могла только река, он гиб бы от жажды посреди
// охоты — на первых прогонах именно так и выходило, хищники умирали с
// полным брюхом и пустой флягой.
constexpr int kWaterPerFood = 6;

// Цена одного шага в энергии (взрослому). Вместе со скоростью из генома это
// и есть плата за быстроту: быстрое животное делает больше шагов за тик,
// значит и тратит больше — скорость покупается не только бюджетом
// преимуществ, но и обменом веществ.
//
// Но плата не должна превосходить саму жизнь. При вдвое большем значении
// шаг стоил дороже, чем существование (energy_upkeep), и поиск еды
// обходился дороже найденного: хищник, обходящий свой участок, сжигал две
// своих ёмкости за тысячу тиков и умирал раньше, чем находил добычу. При
// вдвое меньшем — наоборот, ходьба переставала что-либо стоить, и стадо,
// которому корм давался даром, за тысячу тиков объедало весь мир.
constexpr int kStepEnergy = 250;

// Водопой: сколько единиц воды животное выпивает за тик. Сколько глубины
// это снимало с клетки, было вторым числом, и подобрано оно было так, чтобы
// след получался неразличимым, — то есть чтобы не значить ничего.
constexpr int kDrinkRate = 4000;

// Ниже этой развитости траву объедать нечего: животное просто не считает
// такое едой. Иначе стадо паслось бы на голых проростках, не давая лугу
// отрасти. Тот же порог для мяса (kMinBiteMeat) живёт вместе с остальным
// законом охоты в core/Hunting.hpp — его спрашивает и наблюдатель, и ради
// одного числа тащить сюда весь закон охоты незачем.
constexpr int kMinBiteGrowth = 50;

// Сколько развитости остаётся на траве, сколько бы ртов над ней ни стояло:
// низ куртины зубами не берётся.
//
// Порога kMinBiteGrowth для этого не хватало, и разница здесь несущая: он
// говорит, с какой травы НАЧИНАЮТ есть, а не до какой её ДОЕДАЮТ. Годность
// в еду спрашивалась один раз, до дележа, а делёж потом раздавал всё, что
// на клетке нашёл: стадо, вставшее на развитость 900, уносило за тик все
// 900 и оставляло голую землю. Отрастать ей приходилось с нуля.
//
// Оставленный низ меняет не скорость отрастания — она в PlantSystem одна
// при любой развитости, — а то, с какого места оно начинается. Разница
// решающая там, где до травы есть дело: посев требует развитости
// kSeedMinGrowth (PlantSystem), и с оставленного низа луг доходит до неё
// вчетверо быстрее, чем с нуля. Выесть под собой землю насовсем стадо
// больше не может.
constexpr int kGrazeLeave = 300;

// Сколько с травы можно скусить прямо сейчас — ОДИН закон на всех, кто её
// ест: и на стадо (AnimalSystem), и на гоблина, перебивающего голод
// травинкой (GoblinSystem). Спрашивается он дважды, и оба раза обязан
// ответить одно и то же: сперва — стоит ли эта клетка того, чтобы к ней
// идти, потом — сколько с неё уйдёт в рты. Разойдись эти два ответа, и
// животное ходило бы кормиться туда, где еды для него нет.
inline int edibleGrowth(int growth) {
    return std::max(0, growth - kGrazeLeave);
}

// Сколько энергии даёт единица травы — и почему не столько же, сколько
// единица мяса, ягод или запаса из кучи (kEnergyPerBiomass выше).
//
// Трава — корм объёмный и бедный: её надо съесть много, чтобы прожить
// день. Этим она и годится в меру поголовья. Ест её в этом мире, по сути,
// одно только травоядное — гоблин щиплет её лишь от голода, — а значит
// цена травы двигает численность стада и больше ничью: выше — стаду хватает
// малого луга, ниже — на каждую голову нужно больше поляны, и лишних голов
// луг не прокормит.
//
// Ради этого одна общая шкала биомассы и разошлась надвое. Мясо, ягоды и
// куча остались на kEnergyPerBiomass намеренно: их цена решает жизнь
// хищника и гоблина, и трогать её, настраивая стадо, значило бы связать две
// настройки молча — поправишь траву, а поедет охота.
constexpr int kEnergyPerGrass = 10;

// Пищеварение: раз во сколько тиков одна крупица белка трогается с места и
// уходит в навоз. Отсюда и необходимость есть постоянно: белок не лежит в
// теле вечно. Срок, а не скорость — поэтому телу не нужен накопитель доли:
// дробное "0.004 крупицы за тик" требовало поля dungPending только затем,
// чтобы дробь дожила до целого. Навоз при этом всё равно выпадает не по
// крупице, а порциями в kDungDrop крупиц: это навоз, а не равномерная
// плёнка по всему маршруту.
constexpr std::uint64_t kDungPeriod = 250;
constexpr int kDungDrop = 2;

// Здоровье — одно на все беды. Голод убивает примерно за
// 1/kStarvationHarm тиков, жажда быстрее — без воды живут меньше, чем без
// еды. Заживление медленнее любого из них: пережитая бескормица не
// забывается мгновенно, а уйти от хищника раненым — ещё не спастись.
//
// Прежде это были две величины: stress копил истощение, health держал раны,
// и обе убивали при своём краю. Один закон, записанный дважды, — а причина
// смерти видна и так, в тот тик, когда животное умирает.
constexpr int kStarvationHarm = 20;
constexpr int kDehydrationHarm = 30;
constexpr int kRecoveryRate = 2;

// Цена потомства. Мать отдаёт детёнышу долю своих запасов и крупицу белка —
// ровно как растение отдаёт семени крупицу минералов. Именно эта цена, а не
// отдельный "лимит поголовья", и сдерживает размножение: после родов матери
// нужно заново отъедаться.
//
// Отец не платит ничего: доля энергии "на ухаживание" (kCourtshipEnergyShare)
// сгорала в никуда и ни на что не влияла — ни на выбор партнёра, ни на
// численность, которую держит цена, уплаченная матерью.
constexpr int kBirthEnergyShare = 450;
constexpr int kBirthWaterShare = 300;
constexpr int kNewbornGrowth = 80;

// --- Размер и то, что из него следует ---
//
// Размер взрослого — черта генома (adult_size), и притом черта-ниша: она
// ничего не стоит в бюджете преимуществ, потому что крупным быть не лучше и
// не хуже, а иначе (core/generation/Genetics.hpp, weight == 0). Платит за
// размер сам мир: крупный больше ест, дольше растёт и требует больше
// вещества.
//
// Ради чего он заведён. Хищнику и гоблину нужна БИОМАССА в тик, а на карте
// видно ПОГОЛОВЬЕ. Пока все взрослые были одного размера, связать одно с
// другим было нечем: единственный способ дать хищникам больше мяса — больше
// голов, и мир зарастал мелочью. Теперь та же кормовая база выражается
// вдвое-втрое меньшим числом зверей покрупнее.
//
// Отсюда же и четыре величины ниже. Все они прежде были отдельными чертами,
// и все они на деле — не выбор вида, а следствие его величины: большое тело
// вмещает больше, требует больше вещества и дольше растёт. Оставлять их
// покупаемыми значило бы позволить виду быть крупным даром.
//
// Постоянные общие для всех трёх диет намеренно: тело у травоядного,
// хищника и гоблина одно и то же (см. шапку файла), и заводить здесь
// исключение по диете значило бы разводить это тело надвое. Разница между
// ними остаётся там, где она и была, — в диапазонах черт: у хищника выше
// полоса размера (он крупнее того, кого ест) и своя полоса расхода.

// Энергии влезает в тело на полный размер. Отсюда и время жизни без еды:
// kEnergyPerSize / energy_upkeep тиков, и величина эта от размера не
// зависит — расход растёт вместе с баком.
//
// Число не подобрано, а выведено: это kMeatPerSize * kEnergyPerBiomass
// (core/Carcass.hpp, выше), то есть ровно энергия туши СВОЕГО размера.
// Тем и держится закон, без которого хищники вымирали в каждом прогоне:
// желудок обязан вмещать добычу целиком, иначе зверь наедается с трети
// туши, бросает остальное, и следующая охота нужна ему раньше, чем он
// успевает окупить прошлую.
//
// Травоядному та же мерка достаётся заодно, и это её цена: бак у него
// вышел заметно больше прежнего, а значит и наполняется дольше — голод у
// стада держится выше при том же корме. Поскольку голод теперь единственный
// регулятор численности (болезни от тесноты больше нет), это ГЛАВНОЕ число
// всей правки: выше — стадо голоднее и плодится реже, ниже — хищник не
// доедает добычу. Встречный рычаг, если стадо перестанет плодиться вовсе, —
// kCalmNeed в AnimalSystem.
constexpr int kEnergyPerSize = 208000;

// Воды — на тот же полный размер.
constexpr int kWaterPerSize = 42000;

// Целых крупиц белка на полный рост. Не меньше одной: тело, которому не
// нужно ни крупицы, росло бы из ничего.
constexpr int kProteinPerSize = 6;

// Какую долю своей жизни существо растёт. Взрослеет всякий к четверти
// предельного возраста, какова бы ни была эта жизнь.
//
// От размера срок взросления больше НЕ зависит, и это исправление, а не
// упрощение. Пока он считался как 2500 * размер, крупный вид взрослел к
// 7500 при жизни в 8947 — то есть проводил в детстве почти всю её и до
// порога размножения (kBreedingGrowth) не доживал вовсе. Беда была
// врождённой: размер — черта-ниша без цены, а предельный возраст покупается
// бюджетом, и разъехаться им ничто не мешало. Вид мог выпасть крупным и
// короткоживущим — мёртвым с рождения, без единого потомка за всю историю.
//
// Долей от собственной жизни такой вид становится НЕВОЗМОЖЕН, а не просто
// редок: до взрослого доживает всякий, кто доживает до трёх четвертей себя.
// А цена величины никуда не делась — за неё по-прежнему платят белком,
// кормом и клеткой луга, на которой крупному может не хватить травы.
constexpr int kMaturityShare = 250;

// Сколько существо отдыхает после родов — доля предельного возраста.
//
// Долей, а не числом тиков, по той же причине, что и взросление: срок
// обязан течь в том же времени, в каком живёт сам вид. Иначе долгожитель,
// которому мир растянули жизнь вдесятеро, начал бы плодиться вдесятеро чаще
// за жизнь, и поголовье поехало бы от одной лишь настройки долголетия.
//
// Вместе с kMaturityShare это и держит численность на месте при любом
// множителе жизни: и детство, и отдых между родами растягиваются ровно во
// столько же раз, во сколько сама жизнь, — значит число потомков за жизнь
// не меняется, растягивается только время.
constexpr int kBirthRestShare = 125;

// Размер тела: от kMinSizeShare доли взрослого у новорождённого до полного
// adult_size у выросшего. Спрашивают его отовсюду, где величина зависит от
// того, насколько существо выросло И насколько оно велико само по себе, —
// расход, укус, шаг, удар, вес туши.
inline int bodySize(int growth, int adultSize) {
    return adultSize * (kMinSizeShare + (kFull - kMinSizeShare) * growth / kFull) / kFull;
}

inline int bodySize(const AnimalComponent& state, const AnimalGenomeComponent& genome) {
    return bodySize(state.growth, genome.adultSize);
}

inline int energyCapacityOf(const AnimalGenomeComponent& genome) {
    return kEnergyPerSize * genome.adultSize / kFull;
}

inline int waterCapacityOf(const AnimalGenomeComponent& genome) {
    return kWaterPerSize * genome.adultSize / kFull;
}

inline int proteinNeedOf(const AnimalGenomeComponent& genome) {
    return std::max(1, kProteinPerSize * genome.adultSize / kFull);
}

// Сколько существо живёт и когда взрослеет — с поправкой на долголетие вида
// (WorldPropertiesComponent, в тысячных: kFull — как записано в геноме,
// 10000 — вдесятеро дольше).
//
// Множитель приходит СНАРУЖИ, а не сидит в геноме, и это не мелочь: умножь
// сам ген — и вложение черты посчитается против неумноженных границ таблицы
// (advantageOf, core/generation/Genetics.hpp), обрежется до единицы, и весь
// бюджет преимуществ поедет молча. Долголетие — свойство мира, как скорость
// мутаций, а не выбор вида.
//
// Число приносит вызывающий — тем же способом, каким feedBody получает цену
// куска: тело по-прежнему не знает ни про диеты, ни про виды.
inline int maxAgeOf(const AnimalGenomeComponent& genome, int lifespan) {
    return std::max(1, static_cast<int>(static_cast<std::int64_t>(genome.maxAge) * lifespan / kFull));
}

inline int maturityAgeOf(const AnimalGenomeComponent& genome, int lifespan) {
    return std::max(1, maxAgeOf(genome, lifespan) * kMaturityShare / kFull);
}

// Сколько тиков мать отдыхает после родов.
inline int birthRestOf(const AnimalGenomeComponent& genome, int lifespan) {
    return std::max(1, maxAgeOf(genome, lifespan) * kBirthRestShare / kFull);
}

// Что время делает с телом за один тик: расход на существование,
// взросление, пищеварение, здоровье.
//
// Смерть здесь НЕ проверяется, и это не забывчивость. Между здоровьем и
// смертью у каждого свои беды: удар (core/Strike.hpp) отнимает здоровье уже
// после общего закона тела. Отними их сам и спроси bodyDied после — иначе
// беда этого тика убьёт только на следующем.
//
// tick и id нужны пищеварению: отсчёт сдвинут на постоянный идентификатор,
// иначе всё поголовье испражнялось бы одним и тем же тиком.
inline void advanceBody(AnimalComponent& state, const AnimalGenomeComponent& genome, int lifespan,
                        std::uint64_t tick, std::uint64_t id) {
    const int size = bodySize(state, genome);

    // Отдых после родов утекает по тику за тик — срок, а не скорость
    // (core/Scale.hpp). Здесь, в теле, а не среди желаний: мать не решает
    // отдыхать, она просто ещё не готова.
    if (state.recovery > 0) {
        --state.recovery;
    }

    // Цена существования. Тратится всегда — стоящее на месте животное
    // тоже живёт.
    state.energy -= genome.energyUpkeep * size / kFull;
    state.water -= genome.waterUpkeep * size / kFull;
    const int ageBefore = state.age;
    state.age += 1;

    // Пищеварение: белок не лежит в теле вечно, часть уходит навозом.
    // Отсюда и постоянная нужда есть, а не только "когда кончилась
    // энергия". Крупица трогается с места раз в kDungPeriod тиков — это
    // срок, а не скорость, поэтому никакого накопителя доли телу не нужно.
    if (state.protein > 0 && (tick + id) % kDungPeriod == 0) {
        --state.protein;
        ++state.dung;
    }

    // Взросление: детёныш дорастает до взрослого примерно к maturityAge, но
    // не выше того, что позволяет накопленный белок — ровно как размер
    // растения ограничен его минералами. Голодное животное не растёт вовсе.
    // Прирост за тик — разность двух целых делений: сколько развитости
    // положено в этом возрасте минус сколько было положено в прошлом. Так
    // дробная скорость (kFull / maturityAge) выражается целыми числами, и
    // телу нечего помнить между тиками — возраст оно и так хранит.
    if (state.growth < kFull && state.energy > 0) {
        const int proteinCeiling = state.protein * kFull / proteinNeedOf(genome);
        const int ceiling = std::min(kFull, std::max(state.growth, proteinCeiling));
        const int maturity = maturityAgeOf(genome, lifespan);
        const int gain = state.age * kFull / maturity - ageBefore * kFull / maturity;
        state.growth = std::clamp(state.growth + gain, 0, ceiling);
    }

    // Здоровье — одно на все беды. Голод, жажда и чужие зубы отнимают его,
    // сытость возвращает. Отдельного счётчика истощения (stress) рядом со
    // здоровьем больше нет: две величины, обе от нуля до единицы, обе
    // убивающие при своём краю, — это один закон, записанный дважды. Отчего
    // именно животное умерло, по-прежнему видно в тот момент, когда оно
    // умирает: пустой желудок, пустая фляга или рана.
    if (state.energy <= 0) {
        state.energy = 0;
        state.health -= kStarvationHarm;
    }
    if (state.water <= 0) {
        state.water = 0;
        state.health -= kDehydrationHarm;
    }
    if (state.energy > 0 && state.water > 0) {
        state.health = std::min(kFull, state.health + kRecoveryRate);
    }
}

// Кончилось ли тело: от старости или оттого, что здоровье вышло. Причины
// разные, исход один — тело ложится падалью (см. enqueueDeath в
// core/Carcass.hpp).
inline bool bodyDied(const AnimalComponent& state, const AnimalGenomeComponent& genome, int lifespan) {
    return state.age >= maxAgeOf(genome, lifespan) || state.health <= 0;
}

// Что делает с телом съеденный кусок. Закон один на все диеты, а вот цена
// куска — нет, и потому её приносит вызывающий: kEnergyPerGrass за траву,
// kEnergyPerBiomass за мясо, ягоды и запас из кучи.
//
// Раньше цена была одна и жила прямо здесь. Разошлась она не по недосмотру:
// биомасса меряется по-прежнему одной шкалой, но кусок травы и кусок мяса
// стоят в этой шкале разного, и именно этой разницей держится поголовье
// стада (см. kEnergyPerGrass). Умолчания у параметра нет намеренно —
// забытое умолчание вернуло бы старое молчаливое равенство, и заметить это
// было бы нечем.
//
// Воду еда даёт по одной цене для всех: трава сочная, в туше есть кровь, и
// наевшийся долго может не искать реку — разной эту величину никто никогда
// не делал.
inline void feedBody(AnimalComponent& state, const AnimalGenomeComponent& genome, int eaten,
                     int energyPerBiomass) {
    if (eaten <= 0) {
        return;
    }
    state.energy = std::min(energyCapacityOf(genome), state.energy + eaten * energyPerBiomass);
    state.water = std::min(waterCapacityOf(genome), state.water + eaten * kWaterPerFood);
}

// Куда девается крупица белка, попавшая в тело: в запас, а если тело больше
// не удержит — насквозь, и ляжет навозом там, где существо окажется. Потолок
// — собственная потребность на полный рост: больше своего размера в себе не
// унесёшь.
inline void takeProtein(AnimalComponent& state, const AnimalGenomeComponent& genome, int grains) {
    const int cap = proteinNeedOf(genome);
    for (int grain = 0; grain < grains; ++grain) {
        if (state.protein < cap) {
            ++state.protein;
        } else {
            ++state.dung;
        }
    }
}

} // namespace goblins

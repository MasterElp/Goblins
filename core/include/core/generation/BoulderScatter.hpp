#pragma once

#include "core/World.hpp"

namespace goblins {

// Один из этапов процедурной генерации мира — формирование природных
// объектов (02_CorePrinciples.md, п.5). Выполняется один раз при
// инициализации, до начала тиков, поэтому это не System, а шаг генерации:
// прямое создание Entity здесь не нарушает правило очереди команд
// (05_Entity.md, п.5), так как оно защищает только от изменения коллекций
// во время их обхода системами внутри тика — на этапе генерации систем
// ещё не выполняется.
//
// Каждый булыжник — непроходимый Entity (04_WorldModel.md, п.4): при
// генерации соблюдается правило "второй непроходимый объект на этот же
// тайл поставить нельзя".
void scatterBoulders(World& world, int count, unsigned seed);

} // namespace goblins

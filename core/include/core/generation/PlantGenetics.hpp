#pragma once

#include <cstddef>
#include <cstdint>
#include <span>
#include <vector>

#include "core/Random.hpp"
#include "core/components/PlantGenomeComponent.hpp"
#include "core/components/SoilComponent.hpp"
#include "core/generation/Genetics.hpp"

namespace goblins {

// Правила, по которым строится, наследуется и читается геном растения. Не System и
// не компонент: это общий для двух вызывающих сторон набор чистых функций
// — генерация мира создаёт этим виды и первые семена
// (generation/GrassSeeding.hpp), а PlantSystem этим же мутирует геном
// потомка при размножении. Держать одни и те же диапазоны и цены в двух
// местах нельзя: они обязаны совпадать, иначе мутация уводила бы потомков
// за пределы, в которых виды изначально сбалансированы.
//
// Сама механика "таблица черт + бюджет преимуществ" здесь не живёт: она
// общая для всего живого и лежит в core/generation/Genetics.hpp (там же
// объяснено, как устроен баланс и почему мутация — всегда обмен). Здесь —
// только то, что относится именно к траве: её таблица черт, её пределы по
// числу видов и её пригодность почвы.
//
// Добавить новую черту — значит дописать строку в таблицу ниже: генерация
// видов, мутация, сохранение и сетевой протокол перебирают таблицу, а не
// перечисляют поля руками.
using PlantTrait = genetics::Trait<PlantGenomeComponent>;

// Трава: первый (и пока единственный) вид растения. Числа подобраны под
// уже существующий мир, а не сами по себе:
// - влажность почвы SoilComponent.moisture живёт в 0..kFull и вдали от
//   воды стремится к фоновой (kBackgroundMoisture), поэтому moistureNeed
//   сравнивается с ней напрямую;
// - минералы SoilComponent.minerals — счётные крупицы, в среднем около
//   mineralsAverage (10) на тайл, поэтому kPlantMinerals ниже измеряется
//   единицами, а не сотнями: одному растению достаётся заметная часть
//   запаса своей клетки, и вернётся она туда же перегноем;
// - доли (развитость, влажность, глубина воды) — в тысячных, см.
//   core/Scale.hpp: 850 в таблице ниже читается как прежние 0.85;
// - времена (maturityAge/maxAge) — в тиках: при tick_interval_ms=200 это
//   от одной до нескольких минут жизни, то есть полный цикл
//   рост -> размножение -> смерть -> перегной виден на глаз.
// Цены (weight) не равны единице у всех черт намеренно. Плодовитость —
// самое сильное преимущество в мире, где свободные клетки кончаются:
// число потомков за жизнь примерно пропорционально произведению
// "как часто сеет" на "сколько лет сеет", поэтому вид, купивший сразу и
// высокий seed_chance, и большой max_age, и раннее созревание, выигрывает
// у всех, что бы они ни купили взамен (это не теория — так и вело себя
// первое поколение этих чисел с одинаковыми ценами). Тройная цена семян
// и полуторная — долголетия делают такой набор неподъёмным: за него
// приходится отдавать почти всё остальное, и вид становится узким
// специалистом, который живёт только у воды на своей почве.
inline constexpr PlantTrait kGrassTraits[] = {
    // Возраст созревания — короткий "юношеский" срок, а не основная
    // дорога к размножению: до первого семени растение обязано ещё и
    // дорасти до kSeedMinGrowth, а это уже дело growth_rate. При широком
    // диапазоне возраста (сотни тиков) созревание всегда наступало позже
    // роста, и growth_rate переставал на что-либо влиять — вид мог купить
    // самый медленный рост без всяких последствий.
    {"maturity_age", &PlantGenomeComponent::maturityAge, 250, 40, 1.25f},
    {"max_age", &PlantGenomeComponent::maxAge, 300, 2500, 1.5f},
    // Тысячных развитости за тик (kFull = взрослое растение).
    {"growth_rate", &PlantGenomeComponent::growthRate, 2, 25, 1.0f},
    // Влажность, при которой вид растёт в полную силу. Диапазон обрезан по
    // тому, что мир реально предлагает: фон вдали от воды — 0.5
    // (kBackgroundMoisture), вплотную к воде — около единицы, камень
    // отнимает до трети. Вид, которому нужно 0.8, живёт только у воды; вид,
    // которому хватает 0.3, растёт где угодно, но платит за это бюджетом.
    // Выше 0.85 требовать нельзя — такой вид оказался бы бездомным не по
    // своей вине.
    {"moisture_need", &PlantGenomeComponent::moistureNeed, 850, 300, 1.25f},
    // Какую глубину воды растение переносит. Верх диапазона — заметно
    // меньше глубины русла: это трава, а не камыш, вид может пережить
    // лужу после дождя, разлив по пойме и подтопленный берег, но не жизнь
    // в реке. Настоящее преимущество (вид переживает то, от чего соседи
    // гибнут), поэтому цена обычная.
    {"water_tolerance", &PlantGenomeComponent::waterTolerance, 20, 400, 1.0f},
    // Верх диапазона выбран так, чтобы ожидание удачного броска (десятки
    // тиков) было сравнимо с временем, за которое растение отращивает
    // потраченную на прошлое семя биомассу. Иначе ожидание перекрывает
    // всё остальное, и единственной по-настоящему ценной чертой
    // оказывается сам seed_chance.
    {"seed_chance", &PlantGenomeComponent::seedChance, 4, 50, 3.0f},
    // Срок покоя семени, оставленного в своей же клетке, когда ронять
    // потомка некуда. Короткий покой выгоден однозначно: семя раньше
    // начинает следить за клеткой и успевает занять её, как только та
    // освободится, — поэтому это черта с направлением, а не ниша. Цена
    // обычная, а не тройная, как у seed_chance: это преимущество работает
    // только на уже заросшем лугу и только на одной клетке — своей
    // собственной, — тогда как частое семя расселяет вид по всей карте.
    // Диапазон соразмерен окну ожидания (kSeedWaitTicks в PlantSystem):
    // при худшем покое семя почти всё отведённое ему время просто спит.
    {"seed_dormancy", &PlantGenomeComponent::seedDormancy, 500, 40, 1.0f},
};

// Сколько крупиц минералов держит в себе полностью выросшее растение.
// Константа, а не черта генома, и рост она больше не ограничивает.
//
// Чертой она была, и черта эта работала в одну сторону: на бедной
// минералами клетке трава упиралась в потолок роста, не дорастала до
// kSeedMinGrowth и не давала ни одного семени — вид просто вымирал там,
// где влаги ему хватало с избытком. Второй мерой места, поверх влажности,
// минералы быть не должны: место оценивает влага, и она одна (см.
// habitatFit ниже — та же история, тот же вывод).
//
// Крупицы при этом никуда не делись из мира: растение по-прежнему набирает
// их из своей клетки по мере роста, отдаёт объевшему его травоядному и
// возвращает перегноем после смерти. Оно просто больше не ЖДЁТ их, чтобы
// расти. Число — прежнее среднее по виду (черта жила в 1..6 при умолчании
// 4), чтобы круговорот минералов остался той же величины, что и был.
inline constexpr int kPlantMinerals = 4;

// Бесплатных черт (weight = 0) в таблице больше нет: единственной такой
// была "своя" каменистость — выбор ниши, а не преимущество. Ниша по
// каменистости ушла вместе с habitatFit, а ниша по влажности бесплатной
// быть не может: сухое место хуже мокрого для всех, и терпеть сухость —
// это преимущество, за которое платят (см. moisture_need выше). Механика
// бесплатных черт в genetics::makeSpecies при этом осталась — она ждёт
// того, у кого ниша появится снова.

inline constexpr std::size_t kGrassTraitCount = sizeof(kGrassTraits) / sizeof(kGrassTraits[0]);

// --- Деревья ---
//
// Таблица вторая, а тип генома тот же (PlantGenomeComponent) — ровно как у
// травоядного с хищником (AnimalGenetics.hpp). И это не небрежность: дерево
// отличается от травы не устройством, а размахом. Те же черты, другие
// пределы.
//
// Главное отличие — срок. Трава живёт 300..2500 тиков, дерево — 4000..30000,
// то есть переживает десятки поколений травы под собой и застаёт несколько
// поколений травоядных. Отсюда и всё остальное: растёт оно во столько же раз
// медленнее, созревает позже и семена роняет реже — иначе долгожитель,
// плодящийся с частотой однолетки, за тысячу тиков занял бы всё, куда
// дотянулся.
//
// Цены те же и по той же причине, что у травы: плодовитость — самое сильное
// преимущество, поэтому seed_chance стоит втрое, а долголетие в полтора
// раза дороже прочего.
inline constexpr PlantTrait kTreeTraits[] = {
    {"maturity_age", &PlantGenomeComponent::maturityAge, 2500, 600, 1.25f},
    {"max_age", &PlantGenomeComponent::maxAge, 4000, 30000, 1.5f},
    // Тысячных развитости за тик. Верх диапазона — вчетверо ниже травяного:
    // дерево набирает свой рост тысячи тиков, а не сотни.
    {"growth_rate", &PlantGenomeComponent::growthRate, 1, 6, 1.0f},
    // Пределы влажности те же, что у травы: и роща, и луг живут на одной
    // карте и меряют её одной меркой. Разделяет их не вода, а корни (см.
    // core/Trees.hpp) — на бедной земле дерево не вырастет и там, где
    // воды вдоволь.
    {"moisture_need", &PlantGenomeComponent::moistureNeed, 850, 300, 1.25f},
    // Дерево переносит воду глубже травы: подтопленную пойму и разлив оно
    // пережидает, стоя в воде, а не гибнет за десяток тиков.
    {"water_tolerance", &PlantGenomeComponent::waterTolerance, 100, 900, 1.0f},
    // Вчетверо реже травяного: у дерева на семена тысячи тиков жизни, а не
    // сотни, и при травяной частоте одно дерево засыпало бы семенами всю
    // свою рощу каждые несколько сотен тиков.
    {"seed_chance", &PlantGenomeComponent::seedChance, 1, 12, 3.0f},
    // Семя дерева лежит дольше травяного: ему и ждать дольше — место под
    // рощей освобождается раз в десятки тысяч тиков, когда падает сосед.
    {"seed_dormancy", &PlantGenomeComponent::seedDormancy, 2000, 200, 1.0f},
};

inline constexpr std::size_t kTreeTraitCount = sizeof(kTreeTraits) / sizeof(kTreeTraits[0]);

// Сколько видов травы может быть в мире. Нижняя граница — чтобы вообще
// было кому конкурировать, верхняя — чтобы при фиксированном бюджете
// виды оставались различимыми (см. genetics::kMinSpeciesDistance).
inline constexpr int kMinGrassSpecies = 3;
inline constexpr int kMaxGrassSpecies = 12;

// Видов деревьев меньше, и нижняя граница — единица: рощ на карте в разы
// меньше, чем травы, и десяток древесных видов на десяток рощ означал бы
// по виду на рощу, то есть никакой конкуренции и никакого смысла в
// бюджете. Верхняя — чтобы виды оставались различимы на глаз.
inline constexpr int kMinTreeSpecies = 1;
inline constexpr int kMaxTreeSpecies = 5;

// Случайность (mixSeed/randomUnit) — общая для всего проекта, см.
// core/Random.hpp: системе запрещено хранить генератор между тиками
// (05_Entity.md, п.3), поэтому она каждый раз собирает его заново из seed
// мира, номера тика и координат клетки.

// Набор архетипов видов травы: count видов (обрезается до
// [kMinGrassSpecies, kMaxGrassSpecies]), у каждого — свой расклад одного
// и того же бюджета преимуществ и своя ниша по каменистости. Виды,
// оказавшиеся слишком похожими на уже созданные, перевыбираются, чтобы в
// мире не было двух почти одинаковых трав.
std::vector<PlantGenomeComponent> makeGrassSpecies(int count, std::uint64_t seed);

// То же самое для деревьев: count видов (обрезается до
// [kMinTreeSpecies, kMaxTreeSpecies]) по таблице kTreeTraits.
std::vector<PlantGenomeComponent> makeTreeSpecies(int count, std::uint64_t seed);

// Геном потомка: геном родителя, слегка сдвинутый случайной мутацией
// (mutationRate — доля вложения, на которую может съехать каждая черта),
// прижатый к полосе вокруг архетипа вида и пересчитанный обратно на тот
// же бюджет. Пересчёт важен не меньше самой мутации: без него удачная
// мутация просто добавляла бы потомку преимуществ, и за несколько сотен
// поколений линия стала бы лучше всех во всём. С ним любая мутация — это
// обмен: где-то прибавилось, значит где-то убыло.
PlantGenomeComponent mutateGenome(const PlantGenomeComponent& parent, const PlantGenomeComponent& archetype,
                                  float mutationRate, std::uint64_t seed);

// Мутация по древесной таблице. Отдельная функция, а не флаг у прежней:
// таблица черт — это и есть ответ на вопрос "чей потомок", и передавать её
// признаком значило бы позволить перепутать (мутировать дерево по травяным
// пределам — молча получить дерево, живущее триста тиков).
PlantGenomeComponent mutateTreeGenome(const PlantGenomeComponent& parent, const PlantGenomeComponent& archetype,
                                       float mutationRate, std::uint64_t seed);

// Функции "насколько почва подходит этому геному" (habitatFit) здесь
// больше нет, и это не пропажа, а срез. Она перемножала два допуска — по
// каменистости и по утоптанности, — то есть отвечала на вопрос "хорошее ли
// это место" вторым способом. Первый способ у мира уже был: влажность.
// Каменистая земля суше (kRockMoistureReduction, core/Moisture.hpp), а на
// сухую землю трава не роняет семени (kSeedMinSupply в PlantSystem.cpp) —
// и голые горы получаются сами собой, из одной причины вместо двух.
//
// Пригодность клетки теперь читается прямо там, где нужна: влажности не
// ниже, чем требует геном (moistureNeed), и вода не глубже, чем он
// переносит (waterTolerance). Порогов kSeedingMinFit/kSeedMinFit не стало
// вместе с самой функцией.

} // namespace goblins

#pragma once

#include <cstddef>
#include <cstdint>
#include <span>
#include <vector>

#include "core/Random.hpp"
#include "core/components/PlantGenomeComponent.hpp"
#include "core/components/SoilComponent.hpp"
#include "core/generation/Genetics.hpp"

namespace goblins {

// Правила, по которым строится, наследуется и читается геном растения. Не System и
// не компонент: это общий для двух вызывающих сторон набор чистых функций
// — генерация мира создаёт этим виды и первые семена
// (generation/GrassSeeding.hpp), а PlantSystem этим же мутирует геном
// потомка при размножении. Держать одни и те же диапазоны и цены в двух
// местах нельзя: они обязаны совпадать, иначе мутация уводила бы потомков
// за пределы, в которых виды изначально сбалансированы.
//
// Сама механика "таблица черт + бюджет преимуществ" здесь не живёт: она
// общая для всего живого и лежит в core/generation/Genetics.hpp (там же
// объяснено, как устроен баланс и почему мутация — всегда обмен). Здесь —
// только то, что относится именно к траве: её таблица черт, её пределы по
// числу видов и её пригодность почвы.
//
// Добавить новую черту — значит дописать строку в таблицу ниже: генерация
// видов, мутация, сохранение и сетевой протокол перебирают таблицу, а не
// перечисляют поля руками.
using PlantTrait = genetics::Trait<PlantGenomeComponent>;

// Трава: первый (и пока единственный) вид растения. Числа подобраны под
// уже существующий мир, а не сами по себе:
// - влажность почвы SoilComponent.moisture нормализована в [0, 1] и вдали
//   от воды стремится к нулю (HydrologySystem), поэтому moistureUptake —
//   доля от неё, а не абсолютная величина;
// - минералы SoilComponent.minerals — счётные крупицы, в среднем около
//   mineralsAverage (10) на тайл, поэтому mineralNeed измеряется
//   единицами, а не сотнями: одному растению нужна заметная часть запаса
//   своей клетки, и вернётся он туда же перегноем;
// - времена (maturityAge/maxAge) — в тиках: при tick_interval_ms=200 это
//   от одной до нескольких минут жизни, то есть полный цикл
//   рост -> размножение -> смерть -> перегной виден на глаз.
// Цены (weight) не равны единице у всех черт намеренно. Плодовитость —
// самое сильное преимущество в мире, где свободные клетки кончаются:
// число потомков за жизнь примерно пропорционально произведению
// "как часто сеет" на "сколько лет сеет", поэтому вид, купивший сразу и
// высокий seed_chance, и большой max_age, и раннее созревание, выигрывает
// у всех, что бы они ни купили взамен (это не теория — так и вело себя
// первое поколение этих чисел с одинаковыми ценами). Тройная цена семян
// и полуторная — долголетия делают такой набор неподъёмным: за него
// приходится отдавать почти всё остальное, и вид становится узким
// специалистом, который живёт только у воды на своей почве.
inline constexpr PlantTrait kGrassTraits[] = {
    // Возраст созревания — короткий "юношеский" срок, а не основная
    // дорога к размножению: до первого семени растение обязано ещё и
    // дорасти до kSeedMinGrowth, а это уже дело growth_rate. При широком
    // диапазоне возраста (сотни тиков) созревание всегда наступало позже
    // роста, и growth_rate переставал на что-либо влиять — вид мог купить
    // самый медленный рост без всяких последствий.
    {"maturity_age", &PlantGenomeComponent::maturityAge, 250.0f, 40.0f, 1.25f},
    {"max_age", &PlantGenomeComponent::maxAge, 300.0f, 2500.0f, 1.5f},
    {"growth_rate", &PlantGenomeComponent::growthRate, 0.002f, 0.025f, 1.0f},
    {"moisture_uptake", &PlantGenomeComponent::moistureUptake, 0.01f, 0.1f, 1.0f},
    {"moisture_capacity", &PlantGenomeComponent::moistureCapacity, 0.05f, 1.2f, 0.75f},
    {"moisture_need", &PlantGenomeComponent::moistureNeed, 0.008f, 0.0008f, 1.25f},
    {"mineral_uptake", &PlantGenomeComponent::mineralUptake, 0.005f, 0.08f, 0.75f},
    {"mineral_need", &PlantGenomeComponent::mineralNeed, 6.0f, 1.0f, 1.0f},
    {"rockiness_tolerance", &PlantGenomeComponent::rockinessTolerance, 0.12f, 0.85f, 1.0f},
    // Верхняя граница — не 1: утоптанность в нынешнем мире выше ~0.5
    // почти не встречается (её размягчает вода, HydrologySystem), и
    // терпимость сверх этого была бы выброшенным бюджетом. Когда по миру
    // начнут ходить и топтать почву, границу нужно будет поднять.
    {"compaction_tolerance", &PlantGenomeComponent::compactionTolerance, 0.1f, 0.7f, 0.75f},
    // Какую глубину воды растение переносит. Верх диапазона — заметно
    // меньше глубины русла: это трава, а не камыш, вид может пережить
    // лужу после дождя, разлив по пойме и подтопленный берег, но не жизнь
    // в реке. Настоящее преимущество (вид переживает то, от чего соседи
    // гибнут), поэтому цена обычная.
    {"water_tolerance", &PlantGenomeComponent::waterTolerance, 0.02f, 0.4f, 1.0f},
    // Верх диапазона выбран так, чтобы ожидание удачного броска (десятки
    // тиков) было сравнимо с временем, за которое растение отращивает
    // потраченную на прошлое семя биомассу. Иначе ожидание перекрывает
    // всё остальное, и единственной по-настоящему ценной чертой
    // оказывается сам seed_chance.
    {"seed_chance", &PlantGenomeComponent::seedChance, 0.004f, 0.05f, 3.0f},
    // Срок покоя семени, оставленного в своей же клетке, когда ронять
    // потомка некуда. Короткий покой выгоден однозначно: семя раньше
    // начинает следить за клеткой и успевает занять её, как только та
    // освободится, — поэтому это черта с направлением, а не ниша. Цена
    // обычная, а не тройная, как у seed_chance: это преимущество работает
    // только на уже заросшем лугу и только на одной клетке — своей
    // собственной, — тогда как частое семя расселяет вид по всей карте.
    // Диапазон соразмерен окну ожидания (kSeedWaitTicks в PlantSystem):
    // при худшем покое семя почти всё отведённое ему время просто спит.
    {"seed_dormancy", &PlantGenomeComponent::seedDormancy, 500.0f, 40.0f, 1.0f},
    // Ниша, а не преимущество — цена 0 (см. выше). Диапазон обрезан по
    // тому, что мир реально предлагает: каменистость на карте — шум
    // вокруг середины, у краёв [0, 1] тайлов почти нет, и вид с такой
    // "своей" каменистостью оказался бы бездомным не по своей вине.
    {"preferred_rockiness", &PlantGenomeComponent::preferredRockiness, 0.15f, 0.85f, 0.0f},
};

inline constexpr std::size_t kGrassTraitCount = sizeof(kGrassTraits) / sizeof(kGrassTraits[0]);

// Сколько видов травы может быть в мире. Нижняя граница — чтобы вообще
// было кому конкурировать, верхняя — чтобы при фиксированном бюджете
// виды оставались различимыми (см. genetics::kMinSpeciesDistance).
inline constexpr int kMinGrassSpecies = 3;
inline constexpr int kMaxGrassSpecies = 12;

// Случайность (mixSeed/randomUnit) — общая для всего проекта, см.
// core/Random.hpp: системе запрещено хранить генератор между тиками
// (05_Entity.md, п.3), поэтому она каждый раз собирает его заново из seed
// мира, номера тика и координат клетки.

// Набор архетипов видов травы: count видов (обрезается до
// [kMinGrassSpecies, kMaxGrassSpecies]), у каждого — свой расклад одного
// и того же бюджета преимуществ и своя ниша по каменистости. Виды,
// оказавшиеся слишком похожими на уже созданные, перевыбираются, чтобы в
// мире не было двух почти одинаковых трав.
std::vector<PlantGenomeComponent> makeGrassSpecies(int count, std::uint64_t seed);

// Геном потомка: геном родителя, слегка сдвинутый случайной мутацией
// (mutationRate — доля вложения, на которую может съехать каждая черта),
// прижатый к полосе вокруг архетипа вида и пересчитанный обратно на тот
// же бюджет. Пересчёт важен не меньше самой мутации: без него удачная
// мутация просто добавляла бы потомку преимуществ, и за несколько сотен
// поколений линия стала бы лучше всех во всём. С ним любая мутация — это
// обмен: где-то прибавилось, значит где-то убыло.
PlantGenomeComponent mutateGenome(const PlantGenomeComponent& parent, const PlantGenomeComponent& archetype,
                                  float mutationRate, std::uint64_t seed);

// Насколько почва клетки подходит этому геному: 0 — расти невозможно, 1 —
// идеальные для него условия. Здесь и только здесь сказано, что означают
// гены preferredRockiness/rockinessTolerance/compactionTolerance, поэтому
// функция живёт рядом с таблицей черт, а не внутри системы: ею одинаково
// пользуются и генерация (куда вообще сажать первые семена), и PlantSystem
// (как быстро растение растёт и куда ему разрешено дать потомка).
//
// Влажность и минералы сюда не входят намеренно: это не свойство места, а
// расход и приход, которые считаются по тикам (растение может пережить
// засуху на своих запасах и умереть на "подходящей" почве без воды).
float habitatFit(const PlantGenomeComponent& genome, const SoilComponent& soil);

} // namespace goblins

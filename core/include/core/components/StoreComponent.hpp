#pragma once

#include "core/Resources.hpp"

namespace goblins {

// Склад на тайле: всё, что сюда принесли и сложили.
//
// Состояние ЗЕМЛИ, а не предмет на ней: компонент ложится на тот же
// терраформирующий Entity тайла, где живут почва, вода, перегной и падаль
// (04_WorldModel.md, п.3). Наличие компонента и есть факт "здесь что-то
// лежит" (02_CorePrinciples.md, п.3) — у чистой клетки его просто нет.
//
// Держит он тот же список ресурсов, что и руки (core/Resources.hpp): всё, что
// носят, можно и складывать. Еда, солома, ветки — и то, что появится потом,
// без единой новой строки здесь.
//
// Принесённое на одну клетку складывается в одну кучу, как складываются две
// туши и два перегноя: иначе у лагеря копились бы десятки отдельных горсток на
// одном тайле.
//
// Куча — первая вещь в этом мире, существующая не потому, что выросла, а
// потому, что её сюда принесли. Всё остальное на земле — трава, дерево,
// падаль, перегной — оказалось там само.
struct StoreComponent {
    Resources stored{};
};

} // namespace goblins

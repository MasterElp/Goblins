#pragma once

namespace goblins {

// Тег-компонент без данных: животное с ним ест растения. Как и
// ImpassableComponent, само наличие компонента и есть факт
// (02_CorePrinciples.md, п.3), а отсутствие означает, что траву это
// животное не ест.
//
// Диета — единственное, чем травоядное отличается от хищника: тело
// (AnimalComponent), геном (AnimalGenomeComponent), желания и размножение
// у них общие, и живут они по одной системе (AnimalSystem). Именно поэтому
// диета выражена компонентом, а не полем "тип животного": тип объекта в
// этом мире не существует, существует набор компонентов.
//
// Ест травоядное только живые растения на своей клетке; падаль ему не еда.
struct HerbivoreComponent {};

} // namespace goblins

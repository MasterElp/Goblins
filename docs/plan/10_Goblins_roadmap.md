# Поселение как следствие поведения: план по шагам

> Рабочая дорожная карта, а не закон мира. Законы живут в `docs/01..10`;
> здесь — порядок, в котором они пишутся, и почему именно такой. Документ
> временный: когда все десять шагов пройдены, от него остаётся только
> `docs/10_Goblins.md`, а он удаляется.

## Состояние

Единица работы — не подпункт и не шаг целиком, а **заход: группа задач,
которая заканчивается запускаемой проверкой**. Отмечай сделанное здесь:
по этой таблице новая сессия понимает, где остановились.

| Шаг | Заход | Задачи | Чем заканчивается | Состояние |
| --- | --- | --- | --- | --- |
| 1 | A | 1.2–1.5 — четыре выноса, гоблинов ещё нет | сборка + прогон на одном seed, числа обязаны совпасть **в точности** | **сделан** |
| 1 | B | 1.6–1.8 — компоненты, генетика, расселение, `GoblinSystem` | сборка, сервер поднимается, гоблины появляются в летописи | **сделан** |
| 1 | C | 1.9 — круг конфигурации | `python3 tools/check_config_roundtrip.py` | **сделан** |
| 1 | D | 1.10–1.11 — протокол, дельта, сохранение | `check_animal_delta.py` + `check_world_save.py` | **сделан** |
| 1 | E | 1.12–1.13 — клиент, документ, changelog | глазами | **сделан** |
| 2 | — | 2.1–2.8 — усталость и отдых | круг поля `fatigue` через дельту | не начат |
| 3 | — | 3.1–3.8 — память места | `tools/watch_goblin_routes.py` до и после | не начат |
| 4–10 | — | см. таблицу «Полный план» ниже | — | не начат |

Заход A стоит держать отдельным всегда: это чистое перемещение кода, и его
проверка — единственная во всём плане, где допустимо требовать точного
совпадения чисел. Смешанный с новым поведением, он эту возможность теряет
навсегда.

**Заход A закрыт.** Вынесены `core/Body.hpp`, `core/Carcass.hpp`,
`core/Desires.hpp`, `core/TileSnapshot.hpp`; `AnimalSystem.cpp` похудел с
1814 строк до 1559. Сверка ядра до и после на seed 831718 при 600 и при
4000 тиках: совпали все шестнадцать величин (поголовье, биомасса, вода,
влажность, минералы, перегной, падаль, высота) и поимённый список животных
с координатами, здоровьем и желанием — 617 и 2055 записей соответственно.
Не порядок величин, а те же самые числа.

Два отступления от первоначального промта, оба вынужденные:

* `advanceBody` **не проверяет смерть** — для неё отдельная `bodyDied`.
  Между здоровьем и смертью у животного вклинивается болезнь от тесноты, и
  она обязана убивать в тот же тик; проверка внутри общего закона сдвинула
  бы смерть на тик вперёд, то есть изменила бы поведение — ровно то, чего
  этот заход не имеет права допустить.
* Гниение падали помечено в `Carcass.hpp` как проходящее **ровно один раз
  за тик**, и владелец прохода назван прямо (`AnimalSystem`). На заходе B
  появится вторая система, которая падаль ест; сгноить её там второй раз
  будет выглядеть безобидно, а туши начнут пропадать вдвое быстрее.

**Заход B закрыт.** Гоблин есть в мире: `GoblinComponent`,
`GoblinDesireComponent`, `GoblinTribesComponent`, `GoblinParams`,
`GoblinGenetics` (таблица `kGoblinTraits`), `GoblinSeeding` (пятая стадия
генерации), `GoblinSystem` (в цикле последней). Документ — `docs/10_Goblins.md`.

Три вещи, которых в промте не было:

* **Пятый вынос — `core/Share.hpp` плюс `feedBody` и `takeProtein` в
  `Body.hpp`.** Без него дележ куста и то, что делает с телом съеденный
  кусок, оказались бы скопированы в `GoblinSystem` слово в слово. Вынесено и
  сверено отдельно, до появления гоблинов в мире: снова точное совпадение.
* **Гоблин протёк в шесть звериных мест.** Он носит `AnimalComponent` и
  `AnimalGenomeComponent`, поэтому всякий перебор «по телу» его захватил:
  счёт поголовья в консоли и в летописи, средний геном, список добычи у
  наблюдателя, поиск наблюдаемого по идентификатору (там читается
  `DesireComponent`, которого у гоблина нет вовсе), запись в файл мира.
  Везде поставлено `entt::exclude<GoblinComponent>` с объяснением причины.
  Из-под самой `AnimalSystem` гоблин выходит сам — она выбирает по
  `DesireComponent`.
* **Летопись поголовья научилась считать гоблинов по племенам** — иначе
  заход было нечем закончить: в протоколе гоблина ещё нет, и увидеть его
  можно только там.

Известная потеря: **сохранённый мир гоблина не содержит.** Записать его
звериной веткой значило бы вернуть из файла травоядного зверя с гоблинским
геномом. Своя запись — заход D; потерять честнее, чем испортить.

**Заход C закрыт.** `GoblinConfig` (tribes, count, mutation_rate) проведён
через весь круг: `Config.hpp` (структура, макрос, поле в `ServerConfig` и в
`RegenerationRequest`, `toRegenerationRequest`/`applyGeneration`) →
`toGoblinParams` в `server/main.cpp` → секции "Goblins" (GENERATION) и
"Goblin life" (WORLD PROPERTIES) в `SettingsPanel.cpp` → `PROBE` в
`check_config_roundtrip.py`. Проверка: `72 проверок, все поля доезжают в
обе стороны` (было 60 до гоблинов). Клиент и сервер собираются чисто.

**`tools/check_animal_delta.py` починен** (отдельной задачей, не частью
захода C). Отказывал он одинаково и на текущем ядре, и на ядре до гоблинов,
то есть к гоблинам отношения не имел. Причина оказалась одна, и это свойство
сервера, а не гонка в скрипте:

> **Сервер не читает команд с того подключения, которое сам заливает.**
> Соединение обслуживается одним потоком, и пока тот занят отправкой дельт,
> входящие кадры того же соединения не разбираются. Измерено: из двадцати
> просьб о паузе не была прочитана ни одна — мир уходил на сотни тиков
> вперёд, а в журнале сервера не появлялось ни строчки о паузе. Просьба
> отвернуться на залитом подключении не срабатывала и через минуту, шестьсот
> кадров продолжали идти. Отставания наблюдателя при этом не было — он
> успевал за рассылкой.

Отсюда всё устройство проверки: она разговаривает по **двум** подключениям —
одно слушает мир и ничего не просит, второе просит и молчит с рождения
(`updates: false` до первой рассылки). По молчащему просьба доходит всегда.

Попутно исправлено ещё четыре места:

* `wait_for` обещал таймаут, но передавал `recv()` без него — замолчавший
  сервер подвешивал проверку насмерть вместо того, чтобы назвать причину;
* между последней дельтой и вставшим миром оставался неразосланный хвост, и
  проверка сличала два разных тика, объявляя расхождением то, что им не было
  (теперь `settle` дожидается тишины);
* полный список выпрашивался ВТОРЫМ подключением — оно надёжно роняло
  первое (`WinError 10054` сразу после первого сличения). Теперь мир целиком
  просит командное подключение, а читается он там же, где и дельты;
* ожидание на одном подключении без чтения второго блокировало рассылку
  всем (`clientsAreBehind`) — теперь при ожидании разгребаются оба.

Мир проверки уменьшен до 96×96 с неторопливым тиком: на суть проверки размер
не влияет (дельта — про учёт номеров, рождения и смерти), а вес каждого кадра
от него зависит напрямую.

Итог: девять прогонов подряд без единого отказа, на текущем ядре и на ядре
до гоблинов. Заход C на эту проверку не опирался — у него своя, `check_
config_roundtrip.py`, и она прошла чисто ещё тогда.

**Заход D закрыт.** Гоблин проехал круг протокола и круг сохранения.

* **Протокол (версия 26).** `GoblinView` в `LayerSnapshot`, сбор в
  `captureLayers`, `goblinToJson`, `goblinsDeltaJson`, `goblins` и
  `goblin_tribes` в `world_init`, `goblins` в дельте, ветка `kind ==
  "goblin"` в `buildWatchedJson`. Список у гоблинов **свой**, а не общий с
  животными: у него племя вместо диеты и своё перечисление желаний, и класть
  его в звериный список значило бы возить каждому зверю поля, которых у него
  нет.
* **Слияние списков вынесено в шаблон** — `creaturesDeltaJson` на сервере и
  `applyCreatureChanges` в клиенте. Оно у зверя и у гоблина совпадает до
  последней строки, и скопировать его вторым разом значило бы завести два
  закона там, где он один: разъехавшись, они поставили бы существо на карте
  не туда, где оно есть в мире.
* **Сохранение.** Тело пишется тем же ключом `"animal"` (компонент у них
  один), а метка `"goblin"`, своя таблица черт для генома и ключ
  `"goblin_desire"` отличают его при чтении.

Появилась третья машинная проверка — **`tools/check_world_save.py`**, и
появилась не для порядка: круга сохранения не проверял до сих пор никто, а
он ловит потери тише всех. Она сразу нашла настоящую: гоблины сохранялись и
загружались, а **племена — нет**. Загруженный мир оставался с населением, но
без архетипов — мутациям стало не вокруг чего гулять (`kSpeciesBand`), и за
поколения племена слились бы в одно. Поголовье при этом сходилось, карточки
сходились, и увидеть потерю было нечем. `GoblinTribesComponent` теперь
пишется и читается.

Сличает она `world_init`, пришедший **в момент загрузки**, а не снятый
позже: загрузка снимает паузу, и мир успевает шагнуть — сравнение через
несколько тиков расходилось бы на клетку и объявляло это потерей.

Итог: три прогона подряд, все три проверки чисто.

**Заход E закрыт — а с ним и весь шаг 1.** Гоблин стал видимым:

* ромб на карте, цвет по племени (`TileColors::goblinTribe` — третья
  палитра, холодная: тёплая охра и красная краснота уже заняты зверьём).
  Форма отличает его от животного, не полагаясь на цвет: кружок и квадрат
  заняты полом зверя, а на мелком тайле цвета сливаются;
* свой выключатель на клавише `7` (`Layers::goblins`, `show_goblins` в
  `ClientConfig`, строка в панели клавиш);
* клик по гоблину открывает его карточку — своя ветка `Target::Kind::Goblin`,
  заголовок с племенем, тело и желания из панели наблюдения;
* свои панели численности и среднего генома — пятыми, рядом с травой,
  рощами, стадом и хищниками. Не линией среди зверья: гоблинов десятки там,
  где животных сотни, и на общей шкале их кривая легла бы в ноль.

Записи в `CHANGELOG.md` и `docs/10_Goblins.md` дописаны. Все три машинные
проверки после захода чисты.

Проверка глазами — за автором: клиент запускается `./run.sh`, гоблины
включаются клавишей `7`.

**Гоблинов сперва не было видно вовсе — ни на карте, ни в графиках.**
Причина оказалась не в гоблинах: `run.sh` брал бинарник сервера первым
попавшимся (`find ... | head -n 1`), и это был `build/server/Debug/server.exe`
многодневной давности, собранный ещё до того, как гоблины появились. Свежий
`Release` лежал рядом и не запускался никогда. Мир при этом поднимался, жил и
выглядел совершенно исправным — искать было негде.

Усугубил это я: проверки знали жёсткий путь `build/server/server`, которого
на Windows не существует, и чтобы они работали, я клал туда копию руками.
Копии остались лежать и устарели.

Исправлено в трёх местах:

* `run.sh` берёт **самый свежий** бинарник, а не первый найденный;
* появился `tools/server_binary.py` — один ответ на вопрос "где собранный
  сервер" для всех проверок, тоже по времени записи. Класть копии в дерево
  сборки больше не нужно, и мои убраны;
* `check_animal_delta.py` и `check_world_save.py` спрашивают путь у него.

Урок общий и не про гоблинов: **"первый попавшийся файл сборки" — это не
выбор, а жребий**, и проигранный жребий выглядит как исправно работающий
мир.

## Контекст

Мир уже живой: рельеф, вода, почва, минералы, трава, деревья, травоядные,
хищники, падаль, круговорот вещества. Гоблинов в нём нет ни одного — слово
`goblin` встречается только в имени проекта и в комментариях-обещаниях
(`SoilComponent.hpp:19`: «когда гоблины пойдут по миру и начнут набивать
тропы, уплотнение стоит вернуть — но с причиной, а не на спец»).

Задача: сделать так, чтобы поселение **возникло само** — из потребностей
отдельных гоблинов, повторяющихся маршрутов и накопленных мест, — а не из
системы, которая говорит «здесь строй деревню». Это прямо
`02_CorePrinciples.md` п.14 («высокоуровневые явления не проектируются
напрямую») и п.15 («вычисляет следующее состояние или имитирует желаемый
результат?»).

Результат — цепочка из десяти шагов, где каждый шаг сам по себе даёт
наблюдаемое поведение, а не «фундамент под будущее».

---

## Что уже есть и что переиспользуем

| Готовый закон | Файл | Как пригодится |
| --- | --- | --- |
| Голод и жажда из тела | `core/Needs.hpp` | гоблин берёт их без единой правки |
| Выбор шага, инерция, память ног, `WalkShy` | `core/Walk.hpp` | ходьба гоблина |
| Дорога волной BFS в пределах видимости | `core/Path.hpp` (`Reach`) | ходьба за целью в обход воды |
| Встреча, зов, выбор пары | `core/Mating.hpp` | размножение гоблинов |
| Бюджет преимуществ, кроссовер, мутации | `core/generation/Genetics.hpp` | геном племён |
| Таблица признаков вида | `core/generation/AnimalGenetics.hpp` | образец для `kGoblinTraits` |
| Дележ спорного куста долями | `ShareIntent` в `AnimalSystem.cpp` | гоблины делят траву и тушу |
| Снимок клеток на весь тик | фаза 2 `AnimalSystem.cpp` | выносим в общий заголовок |
| Тело: расход, взросление, здоровье, смерть | фаза 3 `AnimalSystem.cpp` | **выносим в `core/Body.hpp`** |
| Арбитраж желаний с инерцией | `chooseDesire`, `AnimalSystem.cpp:398` | **выносим в `core/Desires.hpp`** |

Три выноса — это не рефакторинг ради красоты. `CLAUDE.md` и комментарий в
`Needs.hpp` говорят одно и то же: две копии одного закона разъезжаются
молча. Тело гоблина и тело животного — один закон.

---

## Принятые решения

1. **Тело общее, система своя.** Гоблин носит `AnimalComponent`,
   `AnimalGenomeComponent`, `MovementComponent`, `IdentityComponent`,
   `PositionComponent` + тег `GoblinComponent`. Закон тела живёт в
   `core/Body.hpp` и вызывается обеими системами. `GoblinSystem` идёт в
   `GameLoop` **после** `AnimalSystem`.
   Желание у гоблина своё (`GoblinDesireComponent`), и именно оно
   автоматически исключает его из `AnimalSystem`: та выбирает по
   `DesireComponent`, которого у гоблина нет.
2. **Гоблин всеяден:** трава (`PlantComponent::growth`) и падаль
   (`CarcassComponent::meat`). Мясо — если видно, иначе трава.
3. **Хищник гоблина не трогает.** Гоблин не попадает в список `preys`, у
   него нет страха и бегства. Иначе численность поселенцев будет держать
   хищник, а не их собственные решения, и наблюдать эмерджентность станет
   нечем. Возвращается это на любом шаге одной строкой.
4. **Полная генетика:** племена (виды), бюджет преимуществ, наследование,
   мутации — своей таблицей признаков `kGoblinTraits`. Отсюда следует, что
   в первый шаг входит и размножение: без рождений наследовать нечего, а
   без наследования генетика не работает вовсе.

---

## Полный план: десять шагов

Цепочка ровно та, что была сформулирована:
потребности → цели → перемещение → повторяющиеся маршруты → места
притяжения → строительство → специализация → социальные связи → поселение.

| # | Шаг | Что появляется в мире | Что должно стать видно |
| --- | --- | --- | --- |
| **1** | **Гоблин существует и живёт** | тело, племена, всеядность, жажда, ходьба, размножение | гоблины пасутся, пьют, доживают до внуков |
| **2** | **Усталость и отдых** | `fatigue`, `Desire::Rest`, качество места для отдыха | гоблин перестаёт ходить и ложится там, где суше и есть тень |
| **3** | **Память места** | `KnowledgeComponent`: 8 мест, сила, затухание | гоблин **возвращается** туда, где было хорошо — маршруты повторяются |
| **4** | **Тропы** | уплотнение почвы от ног + притяжение утоптанного в `Walk.hpp` | тропы проступают на карте и держат сами себя |
| **5** | **Места притяжения** | отдых предпочитает утоптанное; сходящиеся тропы | лагерь возникает на перекрёстке, никем не назначенный |
| **6** | **Ноша и запас** | `InventoryComponent`, цена переноски | еда оседает у места отдыха — появляется куча |
| **7** | **Работа и постройка** | `WorkComponent`, стройплощадка (п.11: работа — единица труда) | навес там, где спали на голой земле; склад там, где куча |
| **8** | **Умения и специализация** | `SkillComponent`, умение растёт от применения | кто чаще собирает — собирает лучше и потому собирает чаще |
| **9** | **Социальные связи** | память о том, кто был рядом; близость от совместной работы | селятся рядом со своими; племена расходятся или смешиваются |
| **10** | **Поселение как явление** | **ничего нового в мире** — только измерение | панель «поселения»: кластеры по утоптанности и постройкам |

Десятый шаг принципиально ничего не добавляет к законам. Если к нему
поселение не видно на карте — значит, оно не возникло, и виноват один из
шагов 3–7, а не отсутствие «системы поселений».

Инструмент измерения появляется на шаге 3 и служит всем шагам 3–5 и 10:
`tools/watch_goblin_routes.py` — считает, какая доля клеток даёт половину
всех посещений. До памяти это число близко к половине (движение размазано),
после памяти и троп обязано резко упасть. Это и есть числовой ответ на
вопрос «маршруты повторяются или мне кажется».

---

# Шаг 1. Гоблин существует и живёт

**Цель шага:** на карте живут гоблины, которые едят, пьют, ходят,
размножаются и умирают, и всё это видно в клиенте — карточка, панель
наблюдения, график численности, геном племени. Ни памяти, ни отдыха, ни
построек здесь ещё нет.

**Почему шаг такой большой:** это единственный шаг, который платит весь
круг (генерация → ядро → конфигурация → протокол → дельта → сохранение →
клиент → проверки). Всё дальнейшее только дописывает поля в уже
проложенный круг.

### Задачи

| № | Задача | Файлы |
| --- | --- | --- |
| 1.1 | Влить `main` | `git fetch origin main && git merge origin/main` |
| 1.2 | Вынести закон тела в `core/Body.hpp`, перевести на него `AnimalSystem` | `core/Body.hpp`, `core/src/systems/AnimalSystem.cpp`, `core/Diagnostics.hpp` |
| 1.3 | Вынести падаль и смерть в `core/Carcass.hpp` | `core/Carcass.hpp`, `AnimalSystem.cpp` |
| 1.4 | Вынести арбитраж желаний в `core/Desires.hpp` | `core/Desires.hpp`, `AnimalSystem.cpp` |
| 1.5 | Вынести снимок клеток в `core/TileSnapshot.hpp` | `core/TileSnapshot.hpp`, `AnimalSystem.cpp` |
| 1.6 | Компоненты гоблина | `components/GoblinComponent.hpp`, `components/GoblinDesireComponent.hpp`, `components/GoblinSpeciesComponent.hpp` |
| 1.7 | Генетика племён и расселение | `generation/GoblinParams.hpp`, `generation/GoblinGenetics.hpp`, `generation/GoblinSeeding.{hpp,cpp}`, `core/src/Simulation.cpp`, `WorldPropertiesComponent.hpp` |
| 1.8 | `GoblinSystem` | `systems/GoblinSystem.{hpp,cpp}`, `core/src/Simulation.cpp`, `core/CMakeLists.txt` |
| 1.9 | Круг конфигурации | `shared/config/Config.hpp`, `server/src/main.cpp`, `SettingsPanel.cpp`, `tools/check_config_roundtrip.py` |
| 1.10 | Круг протокола и дельты | `server/NetworkServer.{hpp,cpp}`, `clients/desktop/src/NetworkClient.{hpp,cpp}`, `tools/check_animal_delta.py` |
| 1.11 | Сохранение мира | `server/src/WorldSave.cpp` |
| 1.12 | Клиент: отрисовка, слой `7`, карточка, графики, геном | `WorldScreen.cpp`, `MapTexture.hpp`, `TileColors.hpp`, `KeysPanel.cpp`, `InfoPanel.cpp`, `PopulationGraph.cpp`, `GenomeGraph.cpp`, `server/src/PopulationHistory.cpp` |
| 1.13 | Документ и changelog | `docs/10_Goblins.md`, `CHANGELOG.md` |

### Промты шага 1

**Промт 1.2 — вынос закона тела**

> Вынеси закон тела животного из `core/src/systems/AnimalSystem.cpp` в новый
> заголовок `core/include/core/Body.hpp` — по образцу `core/Needs.hpp`: один
> закон, два вызывающих. В него уходит фаза 3 «Тело» целиком, кроме болезни
> от тесноты (она про своих сородичей и к гоблинам не относится — остаётся в
> `AnimalSystem`).
>
> API:
> ```cpp
> int bodySize(int growth);                          // kMinSizeShare..kFull
> bool advanceBody(AnimalComponent&, const AnimalGenomeComponent&,
>                  std::uint64_t tick, std::uint64_t id);  // false = умерло
> ```
> Внутрь переезжают: расход энергии и воды на существование (умноженный на
> размер), `age += 1`, пищеварение по периоду `kDungPeriod`, взросление
> разностью двух делений с потолком по белку, вред от истощения и
> обезвоживания, заживление, проверка смерти по `maxAge` и `health`.
> Константы `kMinSizeShare`, `kDungPeriod`, `kDungDrop`, `kStarvationHarm`,
> `kDehydrationHarm`, `kRecoveryRate`, `kEnergyPerBiomass`, `kWaterPerFood`,
> `kStepEnergy`, `kDrinkRate`, `kMeatPerSize`, `kBirthEnergyShare`,
> `kBirthWaterShare`, `kNewbornGrowth` переезжают туда же.
>
> Комментарии, объясняющие ПОЧЕМУ у константы такое значение и что ломалось
> при другом, переезжают вместе с константами дословно — это самое ценное,
> что есть в этих строках (`CLAUDE.md`, раздел про комментарии).
>
> Экспорт констант наблюдателю не должен пропасть: `appendAnimalSystemConstants`
> в `core/src/Diagnostics.cpp` продолжает отдавать те же имена в той же
> группе (добавь `appendBodyConstants` и вызови её оттуда).
>
> Проверка обязательна и она строгая: это чистый вынос, ни одно число не
> меняется, поэтому мир на том же seed обязан вести себя **в точности так
> же**, а не «в том же порядке величин». Собери до и после, прогони 8000
> тиков на одном seed и сверь численность травоядных, хищников, воду и
> минералы — расхождение хоть в единицу означает, что при выносе что-то
> потерялось.

**Промт 1.3 — падаль и смерть**

> Вынеси из анонимного пространства имён `AnimalSystem.cpp` в новый
> `core/include/core/Carcass.hpp` три функции: `depositCarcass`,
> `releaseCarcassProtein`, `enqueueDeath`. Причина — гоблин умирает тем же
> способом и ложится той же тушей; закон смерти в мире один.
> Комментарии переносятся дословно. Поведение не меняется ни на единицу.

**Промт 1.4 — арбитраж желаний**

> Вынеси арбитраж желаний из `chooseDesire` (`AnimalSystem.cpp:398`) в
> `core/include/core/Desires.hpp`. Закон общий, набор желаний — нет: у
> животного их пять, у гоблина их станет восемь, и общий перечень был бы
> враньём для обоих.
> ```cpp
> struct Urgency { int tag; int value; };
> // Побеждает последний из равных: порядок в списке и есть приоритет при
> // равенстве (у животного страх стоит последним — «сытость подождёт,
> // зубы нет»).
> int chooseUrgent(std::span<const Urgency> candidates, int currentTag,
>                  int currentUrgency, int floor, int switchGap, int idleTag);
> ```
> `kDesireFloor` и `kDesireSwitch` остаются в `AnimalSystem` (у гоблина
> будут свои). `chooseDesire` переписывается поверх и обязан выбирать ровно
> то же самое — проверь тем же прогоном на одном seed.

**Промт 1.5 — снимок клеток**

> Вынеси фазу 2 `AnimalSystem` («Снимок клеток») в
> `core/include/core/TileSnapshot.hpp`:
> ```cpp
> struct TileSnapshot {
>     int width = 0, height = 0;
>     std::vector<entt::entity> terrain, plantAt;
>     std::vector<int> waterAt, plantGrowth, carcassMeat;
>     std::vector<std::uint8_t> treeAt;
>     void capture(const World&);          // переиспользует свои массивы
>     std::size_t index(int x, int y) const;
> };
> ```
> Снимок снимает КАЖДАЯ система себе и в начале своего прохода, а не один
> на весь тик: `GoblinSystem` идёт после `AnimalSystem` и обязана видеть
> траву такой, какой её оставило стадо. Объект живёт у вызывающей стороны и
> переиспользуется между тиками, как `Reach`.

**Промт 1.6 — компоненты гоблина**

> Добавь компоненты гоблина, по образцу существующих и с комментариями,
> объясняющими причину:
>
> * `core/include/core/components/GoblinComponent.hpp` — пока пустой тег,
>   как `HerbivoreComponent`. В комментарии объясни: гоблин — не третья
>   диета, а существо, чьи решения растут из памяти и мест; тело у него то
>   же самое, и потому он носит `AnimalComponent`.
> * `core/include/core/components/GoblinDesireComponent.hpp` —
>   `enum class GoblinDesire : std::uint8_t { Idle, Food, Water, Mate }`,
>   `goblinDesireName` / `goblinDesireFromName` (рядом с перечислением, как
>   в `DesireComponent.hpp`), и `struct GoblinDesireComponent { int mating;
>   GoblinDesire current; }`.
>   В комментарии объясни, почему перечисление своё, а не общее с
>   `Desire`: у гоблина желаний станет вдвое больше, и животное носило бы
>   значения, которые не может выбрать никогда.
>   Отдельно отметь второе следствие: именно отсутствие `DesireComponent`
>   и выводит гоблина из-под `AnimalSystem` — та выбирает по нему.
> * `core/include/core/components/GoblinSpeciesComponent.hpp` — образцы
>   племён на Entity мира, по образцу `AnimalSpeciesComponent`.

**Промт 1.7 — генетика племён и расселение**

> Сделай генетику и расселение гоблинов по образцу животных:
>
> * `core/include/core/generation/GoblinParams.hpp` —
>   `struct GoblinParams { int tribes = 2; int count = 30; int mutationRate = 60; }`
>   с пределами `kMinGoblinTribes = 1`, `kMaxGoblinTribes = 6`.
> * `core/include/core/generation/GoblinGenetics.hpp` — таблица
>   `kGoblinTraits` по образцу `kHerbivoreTraits`, и `goblinTraits()` для
>   панели генома. Гоблин покупает: `maturityAge`, `maxAge`, `speed`,
>   `perception`, `energyCapacity`, `energyUpkeep`, `biteSize`,
>   `waterCapacity`, `waterUpkeep`, `proteinNeed`, `breedingUrge`.
>   `attack`, `defense`, `goreChance` он не покупает и они остаются нулями:
>   зубов и рогов у него нет, а то, чем он возьмёт своё, появится на шагах
>   7–8 и будет куплено не бюджетом, а трудом.
>   Диапазоны выбери так, чтобы гоблин был заметно медленнее хищника и
>   заметно дольше жил, чем травоядное: он должен успеть привыкнуть к месту.
>   Каждый диапазон снабди комментарием, почему он такой.
> * `core/src/generation/GoblinSeeding.cpp` + заголовок —
>   `seedGoblins(World&, const GoblinParams&, unsigned seed)` по образцу
>   `AnimalSeeding`: ставить только на проходимые клетки, поближе к траве.
> * `core/src/Simulation.cpp` — стадия 5 генерации, после `seedAnimals`
>   (`02_CorePrinciples.md`, п.5: разум возникает в уже сложившемся мире).
>   Смещение seed'а `kGoblinSeedOffset = 3500`.
> * `WorldPropertiesComponent` — `goblinMutationRate`, `goblinRandomSeed`
>   рядом с животными.
> * `core/CMakeLists.txt` — списки файлов явные, глобов нет, допиши руками.

**Промт 1.8 — GoblinSystem**

> Напиши `core/src/systems/GoblinSystem.cpp` и заголовок к ней. Держи ту же
> дисциплину, что у `AnimalSystem`, и по тем же причинам:
>
> * два прохода — сперва весь мир узнаёт, чего хочет, и только потом
>   кто-либо что-либо делает (иначе решение зависит от порядка в хранилище
>   EnTT, `04_WorldModel.md` п.8);
> * система не помнит ничего между тиками (`05_Entity.md` п.3), случайность
>   выводится из `mixSeed(goblinSeed, mixSeed(tick, id))`;
> * структурные изменения — только через `CommandQueue`;
> * спор за один куст и одну тушу решается долями (`ShareIntent`), а не
>   порядком обхода.
>
> Порядок фаз: снимок гоблинов → `TileSnapshot::capture` → тело
> (`advanceBody`, смерть через `enqueueDeath`) → потребности (`hungerOf`,
> `thirstOf` из `core/Needs.hpp`, желание пары копится как у животных) →
> выбор желания (`chooseUrgent`) → решение → дележ еды и воды → шаги
> (`chooseStep` из `core/Walk.hpp`) → встречи и рождения (`core/Mating.hpp`,
> кроссовер и мутация из `core/generation/Genetics.hpp`) → всё.
>
> Всеядность: если в видимости есть падаль — идти к ней, иначе к траве.
> Мясо под ногами едят раньше травы под ногами. Объясни в комментарии,
> почему именно так (туша — это десяток кустов разом, `kMeatPerSize`), и
> почему гоблин при этом не хищник: он не бьёт живое, он подбирает мёртвое.
>
> Зарегистрируй систему в `core/src/Simulation.cpp` **после** `AnimalSystem`
> и объясни в комментарии почему: гоблин ходит по миру, уже прожившему тик,
> и видит траву такой, какой её оставило стадо.
>
> Отдельно отметь в комментарии к системе, что гоблин пока не добыча:
> в списке `preys` у `AnimalSystem` его нет, страха и бегства у него нет.
> Это временное решение, и причина названа — численность поселенцев должны
> держать их собственные решения, а не хищник, пока мы смотрим, возникает ли
> поселение вообще.

**Промт 1.9 — круг конфигурации**

> Проведи параметры гоблинов через весь круг настроек — список мест в
> `CLAUDE.md` (таблица в разделе про новый параметр конфигурации):
> `config.json` → `ServerConfig` → `RegenerationRequest` → панель → обратно.
>
> * `shared/config/Config.hpp`: `struct GoblinConfig { int tribes = 2;
>   int count = 30; int mutation_rate = 60; }`, макрос
>   `NLOHMANN_DEFINE_TYPE_NON_INTRUSIVE_WITH_DEFAULT`, поле в `ServerConfig`
>   и в `RegenerationRequest`, перекладывание в `toRegenerationRequest` и
>   `applyGeneration`.
> * `server/src/main.cpp`: `toGoblinParams`.
> * `clients/desktop/src/SettingsPanel.cpp`: раздел `Goblins` в GENERATION
>   (tribes, initial head count) и строка mutation rate в WORLD PROPERTIES —
>   ровно как у животных, одним вызовом `ops.intRow` на параметр.
> * `tools/check_config_roundtrip.py`: допиши `"goblins"` в `PROBE`
>   значениями, заведомо не равными умолчаниям.
>
> Существующий `config.json` пользователя не трогай и не пересоздавай: в нём
> подобранные вручную значения. Новые поля появятся в нём сами при первом
> сохранении, потому что у макроса есть умолчания.
>
> Проверь машинно: `./build.sh && python3 tools/check_config_roundtrip.py`.

**Промт 1.10 — круг протокола и дельты**

> Проведи гоблина через круг «карточка → дельта → клиент» (таблица в
> `CLAUDE.md`, раздел про новое поле животного). Список гоблинов **свой**,
> отдельный от `animals`: поля у них уже расходятся (племя вместо диеты), а
> дальше разойдутся сильнее — усталость, ноша, работа. Дописывать это в
> `AnimalView` значит раздувать каждое животное полями, которых у него нет.
>
> * `server/include/server/NetworkServer.hpp`: `struct GoblinView { id, x, y,
>   growth, health, GoblinDesire desire, int tribe, Sex sex; }` и
>   `std::vector<GoblinView> goblins` в `LayerSnapshot` (+ очистка в
>   `resize`). Не забудь про инициализацию по именам полей — у соседнего
>   `AnimalView` это сделано ровно потому, что позиционная тихо меняет
>   местами `species` и `growth`.
> * `server/src/NetworkServer.cpp`: сбор в `captureLayers` с сортировкой по
>   `id` и **только** по нему; `goblinToJson`; `goblinsDeltaJson` слиянием
>   двух отсортированных списков, зеркально `animalsDeltaJson` (`pos`,
>   `growth`, `health`, `desire`, `gone`, `born` — порядок ключей и есть
>   обязательный порядок применения); `goblins` и `goblin_tribes` в
>   `buildInitMessage`; `goblins` в `buildDeltaMessage`; ветка
>   `kind == "goblin"` в `buildWatchedJson`.
>   Племя и пол в дельту не попадают: за жизнь они не меняются.
> * `clients/desktop/src/NetworkClient.cpp`: `parseGoblin`,
>   `applyGoblinChanges` в том же порядке, `WorldState::goblins`.
> * `tools/check_animal_delta.py`: расширь на второй список. Не заводи
>   второй скрипт — вся его механика (пауза, вторая связь, сверка на одном
>   тике) нужна обоим спискам одинаково, а два скрипта разъедутся.
>
> Проверь машинно: `./build.sh && python3 tools/check_animal_delta.py`.

**Промт 1.11 — сохранение мира**

> `server/src/WorldSave.cpp` перечисляет компоненты поимённо, поэтому
> гоблина он сейчас сохранит как животное (у него есть `AnimalComponent`) и
> восстановит без тега — то есть потеряет. Добавь `GoblinComponent`,
> `GoblinDesireComponent`, `GoblinSpeciesComponent` в запись и в чтение и
> убедись, что животные и гоблины при чтении не путаются: тег решает.
> Как и у животных, не записывай то, что пересчитывается на первом же тике.
> Проверь кругом: создать мир → сохранить → загрузить → численность гоблинов
> и их племена совпали.

**Промт 1.12 — клиент**

> Покажи гоблинов в клиенте:
>
> * Отрисовка в `WorldScreen.cpp` рядом с животными, но формой отличимой с
>   первого взгляда — ромб. Цвет по племени: `TileColors::goblinTribe` в
>   `TileColors.hpp`, палитра, не пересекающаяся ни с тёплой охрой
>   травоядных, ни с холодной краснотой хищников. Размер от `growth`,
>   полоска здоровья по тем же правилам, что у животных.
> * Свой слой на клавише `7`: поле `goblins` в `MapTexture::Layers` **и в
>   его `operator==`** (без второго текстура не перерисуется при
>   переключении), `show_goblins` в `ClientConfig`, обработчик клавиши со
>   сразу же сохраняемой настройкой, строка в `KeysPanel.cpp`.
> * Карточка наблюдения в `InfoPanel.cpp` для `kind == "goblin"`: тело,
>   потребности, желание, племя.
> * `server/src/PopulationHistory.cpp` — считать гоблинов; линия в
>   `PopulationGraph.cpp`; племена в `GenomeGraph.cpp` по
>   `goblin_tribes` из `world_init`.

**Промт 1.13 — документ**

> Напиши `docs/10_Goblins.md` по образцу `docs/09_Animals.md`: что такое
> гоблин, почему тело у него то же, а система своя, таблица признаков с
> диапазонами и объяснением каждого, всеядность, место в тике, и раздел
> «известные ограничения» — честно: памяти нет, отдыха нет, ноши нет,
> построек нет, хищник его не видит. Каждое ограничение с указанием шага, на
> котором оно снимается.
> Добавь запись в `CHANGELOG.md` в разделе «Добавлено», в принятом здесь
> длинном стиле: что сделано, почему, какие имена в коде, ссылка на документ.

---

# Шаг 2. Усталость и отдых

**Цель шага:** у гоблина появляется первая потребность, которую
удовлетворяет **место**, а не ресурс. Траву съедают, вода течёт — а место
для отдыха никуда не девается, и именно к нему можно вернуться. Без этого
шага возвращаться некуда и поселение возникнуть не из чего.

**Закон:**

* `GoblinComponent` перестаёт быть пустым: `int fatigue = 0` (0..`kFull`).
  Растёт целыми числами за тик: больше за сделанный шаг, меньше за стояние.
  Падает, пока гоблин отдыхает. Никаких дробей и накопителей
  (`core/Scale.hpp`).
* `GoblinDesire::Rest` — срочность равна самой усталости, читается прямо.
* **Качество места для отдыха** — новый закон в `core/Rest.hpp`, и у него
  сразу два вызывающих: система (выбрать, куда лечь) и наблюдатель
  (нарисовать карту пригодности). Складывается из того, что в мире уже
  есть: сухость почвы (`SoilComponent::moisture`), тень дерева
  (`treeAt` — деревья уже служат укрытием в `Hunting.hpp`), отсутствие
  падали под боком. Каменистость — против.
  Это тот самый крючок, в который на шаге 4 добавится утоптанность, а на
  шаге 7 — построенный навес. Заложи его так, чтобы слагаемое добавлялось
  одной строкой.
* Решение: если под ногами достаточно хорошо — стоять и восстанавливаться
  (`busy`, шага нет); иначе `findNearest` по лучшему качеству в пределах
  видимости; если ничего не найдено — брести (`roamDirection`).

**Задачи**

| № | Задача |
| --- | --- |
| 2.1 | Влить `main` |
| 2.2 | `fatigue` в `GoblinComponent`; рост и убыль в `GoblinSystem` |
| 2.3 | `core/Rest.hpp`: `restQualityOf(...)` + константы, экспорт в `Diagnostics` |
| 2.4 | `GoblinDesire::Rest`: перечисление, имена, арбитраж |
| 2.5 | Круг поля: `GoblinView::fatigue` → `captureLayers` → `goblinToJson` → `goblinsDeltaJson` → `parseGoblin` → `applyGoblinChanges` |
| 2.6 | Сохранение `fatigue`; карточка в `InfoPanel` |
| 2.7 | Наложение «пригодность для отдыха» у наблюдаемого гоблина (по образцу `reach`) |
| 2.8 | `docs/10_Goblins.md` + `CHANGELOG.md` |

**Промт 2.2–2.4 — усталость и закон места**

> Добавь гоблину усталость и отдых.
>
> `GoblinComponent` получает `int fatigue = 0` (0..`kFull`). Растёт целыми
> числами за тик — больше за сделанный шаг, меньше за стояние на месте, —
> и падает, пока гоблин отдыхает. Подбери три константы так, чтобы гоблин
> успевал дойти до еды и обратно, не падая с ног, а полный отдых занимал
> заметное, но не разорительное время; каждую снабди комментарием, из чего
> её значение получилось. Дробей и накопителей быть не должно
> (`core/Scale.hpp`).
>
> Новый заголовок `core/Rest.hpp` — закон «насколько эта клетка годится,
> чтобы лечь»: сухость почвы, тень дерева, отсутствие падали, каменистость
> в минус. Один закон, два вызывающих: `GoblinSystem` и наблюдатель, который
> будет рисовать эту карту. Слагаемые складываются целыми числами и так,
> чтобы на шаге 4 добавилось ещё одно (утоптанность), а на шаге 7 — ещё
> одно (навес): оформи это как явный список слагаемых, а не как одно
> выражение.
>
> `GoblinDesire::Rest` со срочностью, равной усталости. Решение: годится
> клетка под ногами — стоять и восстанавливаться, не делая шага; не годится
> — `findNearest` лучшей в пределах видимости; ничего не нашлось — брести.
>
> Смысл шага, отметь его в комментарии к `Rest.hpp`: это первая
> потребность, которую удовлетворяет место, а не вещество. Траву съедают,
> и она кончается; место остаётся, и к нему можно вернуться. Всё поселение
> вырастет из этого различия.

**Промт 2.5–2.7 — круг поля и наблюдение**

> `fatigue` меняется за жизнь, значит он обязан проехать не только карточку,
> но и дельту (`CLAUDE.md`, таблица про новое поле). Проведи его через
> `GoblinView` → `captureLayers` → `goblinToJson` → `goblinsDeltaJson` →
> `parseGoblin` → `applyGoblinChanges`, добавь в сохранение мира и в
> карточку `InfoPanel`.
> Сделай наложение пригодности для отдыха вокруг наблюдаемого гоблина — по
> образцу наложения `reach`, тем же путём через `buildWatchedJson`. Считает
> его сервер по `core/Rest.hpp`, тому же самому, по которому гоблин
> принимает решение: разъехаться им негде.
> Проверь машинно: `./build.sh && python3 tools/check_animal_delta.py`.

---

# Шаг 3. Память места

**Цель шага:** гоблин начинает **возвращаться**. Это первое, чем он
отличается от животного (`docs/09_Animals.md`, §16: «памяти нет вовсе»), и
это же — источник повторяющихся маршрутов. Пока гоблин ходит только туда,
что видит, маршруты случайны; как только он помнит, где вода и где он
спал, дорога между этими точками начинает протаптываться сама.

**Закон:**

* `KnowledgeComponent` — ровно `kKnownPlaces = 8` мест, массив
  фиксированного размера. Ограничение — часть закона, а не оптимизация:
  голова не растёт. Каждое место: клетка, вид (`Food`, `Water`, `Rest`),
  сила 0..`kFull`.
* `core/Knowledge.hpp` — четыре действия, общие для системы и наблюдателя:
  * `remember` — усилить существующее место или занять слот; если слотов
    нет, вытеснить самое слабое, и только если новое сильнее;
  * `forget` — затухание на целую единицу за тик: место, к которому не
    возвращались тысячу тиков, забывается. Никакого накопителя;
  * `recall` — лучшее вспомненное место данного вида: сила минус
    расстояние с весом. «Лучшее», а не «ближайшее»: память тем и отличается
    от зрения, что в ней место сильное может перевесить место близкое;
  * `disappoint` — пришёл, а там пусто: резко ослабить. Так объеденная
    поляна перестаёт быть едой, и не потому, что кто-то её вычеркнул.
* Встраивание в `GoblinSystem`: поел / попил / отдохнул — `remember` клетку
  соответствующего вида. Ничего нужного не видно — `recall`; вспомнил —
  идти туда **напрямик, вслепую обходя преграды**, а не по дороге:
  `Reach` не выходит за пределы видимости, и это верно по сути — гоблин
  помнит, ГДЕ, но не помнит КАК. Память пуста — брести, как раньше;
  блуждание и есть способ её наполнить.
* Память в дельту не идёт: она большая и нужна поштучно. Она идёт в
  `watched` — и наблюдатель рисует помеченные места наблюдаемого гоблина
  цветом по виду и прозрачностью по силе. В сохранение мира — идёт.

**Задачи**

| № | Задача |
| --- | --- |
| 3.1 | Влить `main` |
| 3.2 | `components/KnowledgeComponent.hpp` |
| 3.3 | `core/Knowledge.hpp`: `remember` / `forget` / `recall` / `disappoint` + константы |
| 3.4 | Встраивание в `GoblinSystem`: запоминание после удачи, `recall` вместо блуждания, `disappoint` по приходе на пустое |
| 3.5 | `watched`: список мест в `buildWatchedJson`, разбор в клиенте, наложение на карте |
| 3.6 | Сохранение памяти в `WorldSave.cpp` |
| 3.7 | **`tools/watch_goblin_routes.py`** — мера повторяемости маршрутов |
| 3.8 | Замер до и после, `docs/10_Goblins.md` + `CHANGELOG.md` с числами |

**Промт 3.2–3.4 — память и её встраивание**

> Дай гоблину память места — то единственное, чего у животного нет вовсе
> (`docs/09_Animals.md`, §16).
>
> `core/include/core/components/KnowledgeComponent.hpp`: массив ровно из
> `kKnownPlaces = 8` записей `{ int x, y; PlaceKind kind; int strength; }`,
> `enum class PlaceKind : std::uint8_t { None, Food, Water, Rest }`.
> Размер фиксирован, и объясни в комментарии почему: ограниченность памяти —
> часть закона, а не экономия. Голова не растёт, и именно поэтому гоблину
> придётся выбирать, что помнить.
>
> `core/include/core/Knowledge.hpp` — `remember`, `forget`, `recall`,
> `disappoint`, как описано выше. Затухание — целая единица за тик, без
> накопителя (`core/Scale.hpp`); при `kFull = 1000` это значит, что место,
> к которому не возвращались тысячу тиков, забыто. `recall` оценивает
> `strength - расстояние * вес`: память тем и отличается от зрения, что
> сильное место может перевесить близкое. Каждую константу — с объяснением.
>
> В `GoblinSystem`: после удачного действия — `remember` этой клетки;
> когда нужного не видно — `recall` вместо блуждания, и идти напрямик,
> вслепую обходя преграды через `chooseStep` (`Reach` за пределы видимости
> не выходит, и это верно по сути: гоблин помнит, где, но не помнит, как).
> Пришёл на вспомненное место, а там пусто — `disappoint`. Память пуста —
> брести, как раньше: блуждание и есть способ её наполнить.
>
> Ничего похожего на «список интересных точек мира», общий на всех, быть не
> должно: знание принадлежит Entity (`02_CorePrinciples.md`, п.6), и два
> гоблина обязаны помнить разное.

**Промт 3.5–3.6 — наблюдение и сохранение**

> Память в дельту не идёт — она большая, а нужна поштучно. Отдай её в
> `watched`: список мест наблюдаемого гоблина в `buildWatchedJson`, разбор
> в `NetworkClient`, наложение на карте в `WorldScreen` рядом с `reach` и
> `road` — цвет по виду места, прозрачность по силе.
> Это не украшение: без этого наложения понять, почему гоблин пошёл именно
> туда, нельзя ничем.
> Сохранение памяти в `WorldSave.cpp` — иначе загруженный мир населён
> гоблинами, забывшими всё, и все тропы начнут набиваться заново.

**Промт 3.7–3.8 — измерение**

> Напиши `tools/watch_goblin_routes.py` поверх `tools/ws_probe.py`:
> подключиться, прогнать N тиков, накапливая по дельтам `goblins.pos`
> счётчик посещений каждой клетки, и напечатать меру сосредоточенности —
> какая доля посещённых клеток даёт половину всех посещений, плюс десяток
> самых хоженых клеток.
>
> Это единая мера для всей средней части плана: до памяти движение
> размазано и доля близка к половине, после памяти обязана заметно упасть,
> после троп (шаг 4) — упасть ещё раз. Прогони её на одном seed на сборке
> ДО шага 3 и ПОСЛЕ и внеси оба числа в `CHANGELOG.md`: этот проект
> записывает измеренное, а не предполагаемое.

---

## Проверка

Каждый шаг заканчивается одним и тем же:

```bash
./build.sh && python3 tools/check_config_roundtrip.py && python3 tools/check_animal_delta.py && python3 tools/check_world_save.py
```

А **чистый перенос кода** — ещё и сверкой двух ядер на одном seed
(`tools/compare_cores.py`, появилась на заходе A). Она принимает два готовых
бинарника и сличает всё, что приехало в `world_init`: каждый слой, каждое
существо поимённо. Список величин в ней не зашит — новый слой или новое
племя она подхватит сама. Ожидание от неё в этом случае однозначное:
совпасть обязано ТОЧНО, расхождение хоть в единицу означает, что при
переносе что-то потерялось.

Плюс по шагам — только то, что проверяется машинно:

* **Шаг 1, заход A.** Строгая сверка выносов (1.2–1.5) на том же seed.
* **Шаг 1, заход C.** `check_config_roundtrip.py`.
* **Шаг 1, заход D.** `check_animal_delta.py`, круг сохранения мира.
* **Шаг 3.** `tools/watch_goblin_routes.py` — маршруты обязаны начать
  повторяться. Это проверка того, что память работает вообще, а не того,
  сколько её нужно.

Ручного «посмотреть глазами» здесь мало нигде: и в конфигурации, и в
дельте, и в маршрутах ошибка выглядит как нормально работающий мир.

### Баланс мира сюда не входит

Численность, устойчивость пищевой цепи, скорость роста поголовья, кто кого
выел — **не предмет этих проверок и не задача исполнителя**. Подбор величин
ведётся вручную и отдельно; код обязан быть верным, а не сбалансированным.

Практическое следствие: прогон, показавший, что кто-то расплодился или
вымер, — это не провал шага и не повод крутить константы. Числа
предъявляются как есть, вместе с тем, что их дало, и на этом работа
исполнителя заканчивается.

---

## Что решено отложить и почему

* **Гоблин не добыча.** Пока смотрим, возникает ли поселение вообще.
  Возвращается добавлением гоблинов в список `preys` и желания `Flee` —
  разумно на шаге 7, когда у поселения появятся стены и повод их иметь.
* **`AnimalComponent` не переименован в `BodyComponent`.** Имя врёт (это
  тело живого, а не «животного»), но переименование задевает три десятка
  файлов, формат сохранения и все панели ради нуля изменений в поведении.
  Отмечено в `docs/10_Goblins.md`, делается отдельно и в тихий день.
* **Болезнь от тесноты на гоблинов не распространяется.** Она про тесноту
  сородичей, а поселение тесно по определению — это его смысл. Если
  скученность и должна чем-то ограничиваться, то не той же болезнью, и
  разбираться с этим надо на шаге 5, когда лагерь появится.
* **Умения, ноша, работа, постройки** — шаги 6–8. Всё это нужно вешать на
  уже возникшее место притяжения, а не наоборот: постройка, поставленная
  раньше, чем гоблины сами облюбовали место, и есть та самая система,
  которая говорит «здесь строй деревню».

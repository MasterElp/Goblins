#pragma once

namespace goblins {

// Единственное место, где живёт номер версии — сервер и клиент собираются
// раздельными CMake-проектами (07_TechStack.md, п.6), но оба берут число
// отсюда, а не из своих отдельных констант, иначе версии в заголовке окна
// и в консоли сервера рано или поздно разошлись бы. Поднимать вручную при
// заметных изменениях — не сборочный номер и не git-хэш: тут важно на
// глаз отличить "это старый .exe" от "это новый .exe", а не отследить
// коммит.
inline constexpr const char* kAppVersion = "0.33.0";

} // namespace goblins
